# ذاكرة مشروع عربنا — CLAUDE_PROJECT_MEMORY

> **هذا الملفّ هو المرجع الدائم، لا سجلّ المحادثة ولا الذاكرة.**
> يُقرأ كاملاً قبل أيّ مهمّة، ويُحدَّث بعد كلّ قرار أو تعديل بلا أن يُطلَب.
> أنشئ بأمر مالك البرنامج في **1 سبتمبر 2026**.

⚠️ **ولا يُحذَف ولا يُستبدَل ولا يُعاد بناؤه من الصفر.** يُضاف إليه ويُصحَّح
ما بطل منه في مكانه، ويبقى تاريخُه.

⚠️ **ولا كلمة سرّ فيه، ولا مفتاح، ولا رمز دخول.** والمستودع علنيّ.

---

## 0) أين يقف هذا الملفّ من الملفّات الأخرى — يُقرأ أوّلاً

**ثلاثة مواضع، ولكلٍّ عمل، ولا يُنسَخ محتوى أحدها في الآخر:**

```
CLAUDE.md                    قواعد المنتجِ والبناء · للمبرمج · 7,698 سطراً
CLAUDE_PROJECT_MEMORY.md     ذاكرةُ العمل: قرارات · أخطاء ودروس · وعود · ناقص · تعليمات دائمة
لوحة-عربنا.html               الخلاصةُ نفسُها بلغة مالك البرنامج · خارج المستودع · له هو
```

⚠️ **وعند التعارض، هذا الملفّ هو المعتمَد** — ثمّ يُصحَّح الآخر فوراً،
**ولا يُترَك موضعان يقولان شيئين.** وهي علّة `esc()` نفسها التي دفع
المستودع ثمنها أربع مرّات.

---

## 1) الهدف الأساسيّ

**«عربنا / ARABNA»** — برنامج ويب (ولاحقاً iOS) للجالية العربيّة في
أمريكا، مبتدَؤه هيوستن.

```
دليل أعمال      485 نشاطاً حقيقيّاً + 29 مخترَعاً للتجربة
سوق             بيع وشراء بين الأفراد · لا مال يُستلَم ولا شحن
فعاليّات ومجلّة
مواقيت الصلاة    محسوبة على الجهاز · بلا شبكة وبلا مكتبة
```

**عربيٌّ أوّلاً، ومفتاح إنجليزيّ كامل.** والدخل: اشتراك المحلّات، وبيع
أماكن إعلانيّة.

**النطاق:** `arabna.app` — اشتُري في 19 أغسطس 2026، ورُبط في 1 سبتمبر 2026.

**التواريخ — مقيسةٌ من المستودع لا من الذاكرة:**

```
15 أغسطس 2026    أوّلُ كومِت في المستودع (abd25da) — رفعٌ يدويٌّ من مالك البرنامج
19 أغسطس 2026    شراءُ arabna.app من Namecheap (حتّى 19 أغسطس 2027 · تجديدٌ تلقائيّ)
1  سبتمبر 2026   ربطُ النطاق · وmain على 0.8.2
```

⚠️ **وبدءُ المحادثات أقدمُ من أوّل كومِت ولا أثرَ له يُقاس** — فلا يُذكَر له
تاريخٌ إلّا إن قاله مالك البرنامج.

---

## 2) القرارات المعتمَدة

### أ) الأسعار — قرار 1 سبتمبر، وملفُّه `415`

⚠️ **ولم ينزل بعدُ إلى المستودع: `js/data.js` ما زال يحمل الأسعار
القديمة.** **فلا يُقرأ سعرٌ من الشيفرة ويُبنى عليه قرار.**

```
الاشتراك الشهريّ            29        (نازل في الشيفرة)
السنويّ                     296       محسوب: 29 × 12 − 15٪
سعر التأسيس                19        والسنة مقدَّماً 228 · وحدُّه تاريخٌ لا عدد (ستّون يوماً)
الشريط الرئيسيّ             بالأيّام   أوّل يوم 25 · كلّ يوم بعده +20 · سقف الطلب 7 أيّام
                                    ⚠️ وسعرُ الشهر يُحذَف منه
الشريط المصغّر              20        شهراً · ⚠️ وسقفه 28 ولا يعلو الاشتراك
شريط السوق                 39        شهراً
شريط التصنيف               45        شهراً · فوق السوق عمداً
مقال برعاية                129       بالقطعة
تمييز إعلان                2 · 5 · 8  كما هي
شارة «نشاط موثّق»            0         مجّانيّة داخل الاشتراك · قرار لا سهو
شريط المجلّة · الفعاليّات · تثبيت فعاليّة   تُخفى — أقسامها فارغة
```

⚠️ **وصورُ المشترك — قرار 3 سبتمبر:** `PLAN_LIMITS.paid.photos = 10`.
**الرقمُ يُكتب مرّةً واحدةً في `js/store.js`** وتحمله النصوصُ بـ`{n}`
يملؤها `planText()`، **و«بلا حدّ» لا تُكتب في أيّ نصّ** — كانت الشيفرةُ
تقول Infinity ونصٌّ أقدمُ يقول عشرة وشاشةُ الرفع تقف عند عشرين بصمت،
ثلاثةُ أجوبةٍ لسؤالٍ واحد. **ورقمٌ يُقال قبل الشراء أصدقُ من وعدٍ بلا حدّ
له حدّ**، و«بلا حدّ» على السيرفر تخزينٌ تدفعه الشركة.

⚠️ **وبطاقةُ الترقية في قائمة الدليل تتكرّر كلّ عشر نتائج، لا مرّةً واحدة
عند الخامسة (575)** — بطلب مالك البرنامج 3 سبتمبر: «يحسّ صاحب البزنس إنّه
لمّا يرقّي حيكون فوق»، **موزوناً بعدم إزعاج الزائر العاديّ** الذي يبحث عن
مطعمٍ أو طبيب وهو أكثريّةُ من يرى هذه القائمة. **وقائمةٌ من عشر نتائجَ أو
أقلّ: بطاقةٌ واحدةٌ في آخرها كالسابق** — تكرارُها في قائمةٍ من أربعٍ يملأ
الشاشة كلَّها.

⚠️ **وبطاقاتُ الترقية لا تقول «الأسعار تظهر بعد إنشاء حساب» (565).**
**القاعدةُ V.01.6 باقيةٌ كما هي** — الزائرُ لا يرى سعراً و`showsPrices()`
تحرس كلَّ رقمٍ تجاريّ — **والذي شيل هو الجملةُ وحدها**: البطاقةُ بابٌ،
والبابُ لا يشرح شروطَ الدخول. **والمفتاحُ `pricesAfterSignup` لم يُحذَف**:
`priceGate()` ما زال يقرؤه، وحذفُه يطبع قفلاً بلا كلمات على `#/subscribe`.

**والقاعدتان الحاكمتان:**

```
لا يعلو سعرُ منتجٍ سعرَ منتجٍ يعطي أكثرَ منه
ولا يعلو سعرُ مدّةٍ قصيرةٍ سعرَ مدّةٍ أطولَ منها في المنتج نفسه
```

⚠️ **ولماذا لا يُباع المصغّر بيوم:** رسوم البطاقة على طلبٍ بدولارين
**18٪**، **وموافقةُ الإدارة على طلبٍ بدولارين تأخذ الدقيقة نفسها التي
تأخذها موافقةٌ على طلبٍ بمئة.** **فوقتُ الإدارة هو الحدُّ الأدنى
الحقيقيّ، لا Stripe.**

⚠️ **ولماذا سقفُ السبعة أيّام:** لا حدَّ أدنى، **والسقفُ هو ما يُحدث
الدوران** — بغيره يحجز واحدٌ ثلاثين يوماً في طلبٍ واحد فيعود القفلُ
بثوبٍ أغلى.

التقييماتُ مجّانيّةٌ لكلّ نشاط — قرارٌ قائم، والشيفرةُ تطابقه، والنصوصُ صُحّحت عليه (555) · والاشتراكُ يضيف: صوراً بلا حدّ · 3 فيديوهات · عروض — الأرقامُ من PLAN_LIMITS لا من الذاكرة
بطاقةُ الترقية في قائمة الدليل مميَّزةٌ بالعين (560): حدٌّ وغسلٌ ذهبيّان، وشريطُ ضوءٍ كلّ 6 ثوانٍ، ونبضٌ حول التاج — ولا حركةَ لمن طلب السكون · بطلب مالك البرنامج «شي بيجذب»

### ب) النطاق — 1 سبتمبر

```
arabna.app          مشترى عند Namecheap · باسم مالك البرنامج شخصيّاً
                    فعّال 19 أغسطس 2026 → 19 أغسطس 2027 · تجديد تلقائيّ
الأساسيّ             arabna.app — لا www
www.arabna.app      يحوّل إلى الأصل بـ307 مؤقّت · ⚠️ لا 308 دائم
السجلّان             A · @ · 216.198.79.1   |   CNAME · www · 5e686c946665b0cf.vercel-dns-017.com.
البريد على النطاق     Private Email من Namecheap · سطر TXT · privateemail._domainkey لا يُمَسّ
```

⚠️ **و307 لا 308 بقرار:** التحويل الدائم يُخزَّن في المتصفّحات بعناد،
**وهو نفسُه ما أوجعنا في الاتّجاه المعكوس.** والمؤقّت يُبقي التراجع
ممكناً.

### ج) البيانات المخترَعة — `510`

```
showDemo            الافتراضيّ صار false · وهجرةٌ بعلامة تطفئه على الأجهزة القديمة
التقييمات المخترَعة   تبقى خلف المفتاح ولا تُحذَف الآن — قرار مالك البرنامج
robots.txt          Disallow: / · مؤقّتٌ ويُفتَح يوم النشر لا قبله
```

⚠️ **وسببُ بقاء التقييمات سليم:** المفتاح **تفضيلُ جهاز**، فما دام
الافتراضيُّ مطفأً **لا يستطيع زائرٌ تشغيلَه ولا رؤيتَها**.

### د) صورة المشاركة

**الشعار وحدَه، بلا سطر نصّ.** **ونصٌّ مرسومٌ داخل صورةٍ لا تصله
دفعة، فيتخلّف** — وقد تخلّف فعلاً: الصورة كانت تقول «في Houston»
والبرنامج يقول «في أمريكا».

### هـ) نموذج الدعم

**يُبنى مع دفعة السيرفر، لا قبلها.** **والبريد لا يُشال يوم ينزل.**

⚠️ **ولماذا لا يُبنى اليوم — مقيس:** `form-action 'none'` في سياسة
الحماية · **صفرُ `<form>` في البرنامج كلِّه** · ولا سيرفر يستقبل.
والبديل خدمةُ نماذجَ خارجيّة، **أي مرورُ رسائل الناس بطرفٍ ثالث**.

### و) الكيان — Arabna LLC قائمة

⚠️ **قامت الشركة.** شهادةُ تأسيسٍ مقبولةٌ من Texas Secretary of State:

    Arabna LLC · Domestic Limited Liability Company
    نافذةٌ من 29 أغسطس 2026

**وهذا يقلب قراراً قائماً:** ما كان يُكتب باسم مالك البرنامج شخصيّاً
(الوثائقُ القانونيّة · المراسلات · التسجيلات) **يُكتب من اليوم باسم
Arabna LLC.**

⚠️ **وما تبقّى بعده، بترتيب المحامي (السؤال 1):** EIN · حسابُ بنكٍ
وStripe وApple باسم الشركة · Operating Agreement · نقلُ النطاق والشيفرة
والشعار والحسابات إلى الشركة بـIP Assignment · محاسبةٌ منفصلة.
**ولا يُخلَط مالٌ شخصيٌّ بمال الشركة.**

⚠️ **وموعدان من رسالة الولاية:** مكتبُ المحاسب (Comptroller) سيراسل
العنوانَ المسجَّل لفتح حساب ضريبة الامتياز · **والوكيلُ المسجَّل والعنوانُ
يُحفَظان دائماً — سقوطُهما يُنهي الكيان.**

### ز) ردُّ المحامي — الجولة الثانية (2 سبتمبر) — سبعةُ «مسموح بشروط»

س19  الشيفرةُ ملكُ الشركة تعاقديّاً، وحمايةُ التأليف أضعفُ في المولَّد آليّاً · سجلُّ مصدرٍ وSBOM ← 545
س20  وثيقةُ نقلٍ واحدة (Founder Contribution, Assignment and Assumption) + جدولُ أصول — قبل أوّل بيعٍ وقبل USPTO · مسودّتُها عند مالك البرنامج
س21  الدليل يبقى · تنبيهٌ معلوماتيّ برأسه بنصّ المحامي (344) · ⚠️ ولا يُرجَّح بين صفحتين رسميّتين متعارضتين — تُعرَضان بتاريخهما
س22  الأعمالُ المنزليّة مسموحةٌ بتحقّقٍ متدرّج · لا «Verified/Licensed» بلا سطرِ تحقّق · منطقةٌ لا عنوان ← دفتر السيرفر
س23  الفعاليّات: حقائقُ ورابط، لا وصفَ ولا صورَ ولا scraping · تنبيهٌ جاهز ← 161
س24  العلامة: بعد وثيقة النقل · بحثٌ شامل · ARABNA كلمةً أوّلاً · ≈ $3,150 لثلاث فئات
س25  سؤالُ Apple بصياغة المحامي · القناة: موعدُ App Review قبل البناء · من حساب الشركة وحدَه

⚠️ **وأربعُ تصحيحاتٍ على ملفّي قبِلتُها:** قيامُ الشركة لا ينقل الأصول ·
«ملكُ المخرجات» ≠ «محميٌّ بالتأليف» · إخلاءُ المسؤوليّة لا يغطّي نصيحةً
شخصيّة · «الدفعُ لا يفتح ميزة» خطأٌ — يفتح موضعاً إعلانيّاً ومدّة.

⚠️ **والمذكّرتان (الجولة الأولى والثانية) لا تدخلان المستودع** — موجّهتان
باسم مالك البرنامج.

### ز٢) لوحةُ الإدارة — 6 سبتمبر، وملفُّها `630`

- **لا قفلَ جهازٍ على اللوحة.** يُلغي قرارَ `620` (قفلان). السبب مقيسٌ بيومٍ
  من الاستعمال: انقفل مالكُ البرنامج خارج لوحته بلا استرجاع، وكلُّ متصفّحٍ
  يحتاج ضبطاً، والقفلُ في تخزين المتصفّح نفسِه الذي فيه الجلسة.
- **الحسابُ هو القفل**: `#/admin` تُفتح بجلسةٍ حيّةٍ لحساب `is_admin = true`
  على الخادم، ويُسأل الخادمُ من جديدٍ عند الباب (`verifyAccountAdmin`).
- **ما يراه المشرف يأتي من الخادم**: الإعلاناتُ تُقرأ بنمط الدليل، والمرشّحُ
  يُترك لـRLS (لا `.eq('status', …)` في أيّ قارئ)، والموافقةُ والرفضُ يصلان
  الخادمَ أوّلاً. ⚠️ **ولا تُشال أيُّ سياسةٍ بإضعافِ أخرى.**
- ⚠️ **شرطُ إطلاقٍ على مالك البرنامج**: `is_admin = true` على صفّه من لوحة
  Supabase — قبلها لا يفتح اللوحةَ أحد.

### ح) قرارات قديمة قائمة

```
توثيق الجوال          مطفأٌ بمفتاح ولا يُحذَف (475) · والتير الثاني يُكسَب بالبريد
تغييرُ الرقم           يؤكَّد بالبريد ما دام المفتاح مطفأً · ويؤكّد التغيير لا الرقم
                      فـphoneVerified يبقى false وtier2By لا يُكتَب 'phone' أبداً
دليلٌ مموَّل           صفّان اثنان فقط أعلى الدليل (dirTop = 2) · والباقي بالمسافة
المدينة المختارة يدويّاً   لم تعد تُقفَل (490) — تُحدَّث تلقائيّاً مع سطر تراجع
لا إشعارات نظام       صفرُ نداءٍ لـNotification وPushManager في js/ وsw.js
```

---

## 3) التعليمات الدائمة من مالك البرنامج

```
١   أيّ فكرة يقولها مبنيّةٌ على ما بعد السيرفر — ولا يُطلَب منه أن يعيدها
٢   كلُّ سؤالٍ منّي يأتي بنموذج اختيار، لا مدسوساً في نصّ
٣   مع كلّ ملفٍّ جديد: سطرُ الطابور ولوحةُ عربنا
٤   الطابور يُقرأ من الفرع، لا من الذاكرة
٥   ملفٌّ لم يصل كود يُصحَّح في مكانه · ووصل يُلغى ويُكتب برقمٍ جديد
٦   ملفٌّ واحدٌ في كلّ رسالة لكود · ولا اثنان يمسّان الملفّ نفسه معاً
٧   «اللي ما بتقدر تكتبه لا تكتبه» — ولا رقمٌ من الذاكرة
٨   كلُّ نقاشٍ ينتهي بقرارٍ أو حقيقة يُكتب في اللوحة بتاريخه · وما لا ينتهي بشيء لا يُكتب
٩   «مقفول» = كومِتُ إغلاقٍ بشبكةٍ كاملةٍ خضراء · وما دونه «نزل شغلُه»
```

**وحدودٌ عليّ لا تُتجاوَز:**

```
لا أحذف مشاريعه على Vercel — هو يحذف
لا أنشئ له كلمة سرّ — لا الآن ولا لاحقاً
لا أطلب ولا أقبل مفتاحاً ولا توكن — والزيب هو البديل
لا أدفع إلى المستودع — كود وحدَه يلتزم ويدفع
لا أكتب اسمه — «مالك البرنامج»
لا أكتب حقل رقم بطاقةٍ في البرنامج — حقل Stripe المستضاف وحدَه
لا أختلق تقييماً ولا أنسخ تقييمات Google أو Yelp ولا أخترع حقائق عن نشاط
```

**والأسلوب:** الردّ في المحادثة بالعامّيّة الشاميّة · **وكلُّ نصٍّ يظهر
في البرنامج بفصحى سهلة، ولا عامّيّة فيه أبداً** · الشهورُ بأسمائها
الأجنبيّة · الأرقام غربيّة · **أسماء الأماكن لا تُترجَم** · وأسماء
المنتجات بالإنجليزيّة · **ولا إيموجي ملوّن في الملفّات** (⚠️ مقبولة).

---

## 4) بنية المستودع — ما يلزم معرفتُه قبل أيّ تعديل

```
index.html              الغلاف · وفيه CSP وثلاثةُ عناوينَ مطلقة (og/twitter)
index-single-file.html  مولَّد — python3 tools/build_single.py
manifest.json           start_url نسبيّ: "./index.html#/home" — ⚠️ لا يُلمَس أبداً
sw.js · js/sw-manifest.js   عامل الخدمة · واسم المخزن من رقم النسخة
robots.txt              Disallow: / مؤقّتاً
vercel.json             رؤوس الحماية · ⚠️ ولا cleanUrls (حُذفت في 485)
js/  13 ملفّاً            data · store · i18n · ui · app · prayer · feasts · synonyms · …
js/screens/ 13 شاشة
tools/e2e/  57 سويتاً     run.sh · والمنفذ 0 لا رقمٌ مكتوب
docs/                   الطابور · الحالة · سجل القرارات · إعادة البناء · تقارير
```

**والثوابت الحاكمة:** `APP_VERSION` و`AD_PRODUCTS` و`AD_SLOTS`
و`SUBSCRIPTION_PRICE` في `js/data.js` · و`YEARLY_DISCOUNT`
و`SUPPORT_EMAIL` و`SUPPORT_PHONE` في `js/store.js`.

**والبذور:** `DEMO_BUSINESSES` (29) و`REAL_BUSINESSES` (485)
و`DEMO_REVIEWS` — وكلُّها في `js/data.js`.

---

## 5) الأخطاء والدروس المستفادة

> ⚠️ **كلُّ سطرٍ هنا وقع فعلاً. والقاعدةُ بلا الحادثة التي ولّدتها تصير
> كلاماً، فتُكتب الحادثة معها.**

| الخطأ | السبب | ما يمنع تكراره |
|---|---|---|
| `610` بنى كتابةَ الإعلان على الخادم ولم يبنِ قراءتَه — فالمشرفُ لم يرَ إعلانَ أحدٍ غيره، والطابورُ لم يعمل مع مستخدمين حقيقيّين قطّ (وُجد على المضيف الحيّ) | نصفُ خطوة: الكتابةُ دخلت النطاقَ والقراءةُ سُحبت منه، ولا سويت يقيس «صفٌّ من متصفّحٍ آخر يصل الطابور» | `v79 · 1` بخادمٍ بديلٍ مشتركٍ بين سياقين يعكس RLS · **وكلُّ كتابةٍ على الخادم تُقابَل بقراءةٍ منه في الدفعة نفسِها** |
| السعرُ أُرسل إلى عمود `numeric` نصَّ عرضٍ بعازلَي bidi («⁦$1,250⁩») فرفضته القاعدة — وكلُّ إعلانٍ مسعَّرٍ فشل حيّاً | البديلُ (`_supabase.mjs`) كان يبتلع أيَّ شيءٍ في أيّ عمود | البديلُ يرفض غيرَ الرقم في `price` كما تفعل القاعدة · **بديلٌ متساهلٌ أخطرُ من غيابه** |
| **قلت «شكّي في التحويل كان غلط»** والتحويل كان قائماً (apex → www بـ308) | وجدتُ تفسيراً لعرَضٍ **ثانٍ** (`og:url` قديم) واستعملتُه لأسحب اكتشافاً عن العرَض **الأوّل** | ⚠️ **تفسيرُ عرَضٍ لا يُلغي دليلَ عرَضٍ آخر.** كلُّ عرَضٍ يُغلَق بدليله |
| **والبحثُ يكون بالنمط لا بالاسم** | مسحُ 570 بُني على أسماء المتغيّرات، ففاته حادي عشرَ لم يظهر إلّا بمسحٍ لا يفترض الاسم — **ثمّ وقع الفحصُ الحارسُ في الخطأ نفسِه**: نمطُه `toast\([^)]*\)` لا يطابق `toast(t('gone'), 'err')` فعدّ 4 حيث 15 | ⚠️ **الصنفُ يُبحَث بالنمط، والنمطُ لا يُبنى على شكل الأمثلة التي أمامك** (572) |
| **عائلةُ «رابطٍ يرجع بصمت» كانت أكبر من تسعة** | مسحتُ الصنفَ بنمطٍ مبنيٍّ على أسماء المتغيّرات التي صادفتها التسعة (`!b`/`!c`/`!a`/`!e`)، فلم يرَ موضعاً اسمُ متغيّره غيرُها | ⚠️ **حين يُكتشف صنفُ خللٍ، يُمسَح المشروع كلُّه على الصنف — والمسحُ يُكتب بحيث لا يعتمد على أسماء ما صادفَه** (571) |
| **أداةُ الفحص تحرس ما تراه وحدَه** | `wiring.mjs` كان يطابق `export function` فقط، فلا يرى دالّةً داخليّةً بلا نداء — و`168` يحسب قائمتَه منها | ⚠️ **قبل الاعتماد على أداة، يُقاس ما لا تفحصه.** الأداةُ تكتشف، والحذفُ في ملفّه (570) |
| **قلت «كود خلّص 495»** وهو لم يُغلَق | قرأت كومِت الشغل وحدَه · **ولاحظتُ غياب كومِت الإغلاق وأنا أقيس وكتبتُ «خلص» على أيّ حال** | ⚠️ **«مقفول» = كومِت إغلاقٍ بشبكةٍ كاملةٍ خضراء.** وعلامةُ `[x]` في الطابور تُوضَع عند الإغلاق لا مع كومِت الشغل |
| **حسبتُ اقتراح التسعير على أسعارٍ قديمة** | قرأت `js/data.js` بدل ملفّ القرار `415` — **و`415` لم ينزل بعد** | ⚠️ **السعرُ يُقرأ من مصدر القرار، لا من حالةٍ لم تُحدَّث** |
| **«الاكتشاف مضمونٌ أصلاً»** | صحيحٌ حرفيّاً وخاطئٌ عمليّاً: البلوك عند y=1,529 أي 1.8 شاشة | ⚠️ **يُقاس الموضع بالبكسل، لا يُفترَض من وجود العنصر** |
| **أعطيت «الضوء الأخضر» لـ`cleanUrls`** فانكسر البرنامج المثبَّت | سألت «هل يفتح في Chromium» بدل «ما قيمةُ `redirected` على الصفّ المخزَّن» | ⚠️ **يُقاس ما تقوله المواصفة، لا ما يتسامح معه متصفّحٌ واحد** |
| **موكب بلا `<meta charset>`** فظهرت العربيّة مشوَّهة | فحصتُ العرضَ وعددَ الأخطاء **ولم أقرأ كلمةً عربيّةً واحدة** | ⚠️ **يُقرأ النصُّ المعروض بالعين، لا تُقاس أبعادُه فقط** |
| **«inRegion من nearestCity»** فبنى كود شرطاً على حقلٍ لا وجود له | صياغةٌ توحي بحقلٍ وهي دالّة | ⚠️ **تُكتب واجهةُ الدالّة كما هي: `inRegion(point)` دالّةٌ مصدَّرة** |
| **بندُ فحصٍ كان سيحمرّ على حقيقةٍ صحيحة** («لا إحداثيّة تتكرّر أكثر من مرّتين» و7 أنشطةٍ على عنوانٍ واحد) | حدٌّ مكتوبٌ بدل حدٍّ مشتقّ | ⚠️ **كلُّ حدٍّ يُشتقّ من البيانات، ولا يُكتب رقماً** |
| **رقمُ سويتٍ مكتوبٌ سلفاً** في `470`/`475` وكان مأخوذاً | كُتب الرقم بدل أن يُقرأ | ⚠️ **رقمُ السويت يُقرأ من المجلّد لحظةَ التنفيذ** |
| **سطرُ طابورٍ متناقض:** «قبل 435» و«رأس المفتوحات» وليسا موضعاً واحداً | كُتب من طابورٍ قديم في الذاكرة | ⚠️ **الطابور يُقرأ من الفرع قبل كلّ سطر** |
| **بالغتُ في حجّة `4.2`** فذكرت ثلاث وظائف «حصريّة» واثنتان منها تعملان على الويب عندنا | لم أفحص الحصريّة، اكتفيتُ بالوجود | ⚠️ **«حصريٌّ للغلاف» يُثبَت بأنّ الويب لا يقدر عليه، لا بأنّه موجود** |
| **نسيتُ أنّ `arabna.app` مشترى** وسألتُ عنه من جديد | ذاكرتي تنتهي مع الجلسة ولم يكن مكتوباً | ⚠️ **هذا الملفّ. وكلُّ حقيقةٍ تُقال مرّةً تُكتب مرّة** |
| **قلتُ إنّ `main` لم يتحرّك وإنّ أربعَ دفعاتٍ مغلقةٍ ليست فيه** — وبنيتُ عليه منعَ نشر اسم النطاق، وكتبتُه في اللوحة وفي هذا الملفّ، وصغتُ سؤالين لكود يتّهمان `main` بالرجوع إلى الوراء. **وكود كان على حقّ: `main` على `0.8.2`** | مرجعُ `refs/remotes/origin/main` في نسختي عالقٌ على كومِتٍ قديم، **و`git fetch origin main --force` لم يكن يصحّحه** — يُرجِع الرقمَ القديم نفسَه | ⚠️ **حالةُ فرعٍ تُسأل من الخادم نفسِه قبل أن تُقال: `git ls-remote origin <فرع>`.** **ونسخةٌ محليّة ليست شهادة، ولو بعد `fetch`.** وإصلاحُ العالق: `git update-ref -d refs/remotes/origin/<فرع>` ثمّ سحبٌ كامل بـ`--force --prune` · ⚠️ **وحين ينفي كودٌ قياساً عندي، يُعاد القياسُ من المصدر قبل الدفاع عنه** |
| **سنٌّ لم تعضّ لأنّ التحوير لم يقع** — هروبُ `$` داخل علامتين مزدوجتين في الصدفة أفسد المرسى | حُكِم على الفحص قبل التحقّق من وقوع الكسر | ⚠️ **يُبرهَن وقوعُ الكسر أوّلاً**، والتحويرُ في ملفٍّ يفشل صراحةً إن لم يجد مرساه — درسُ `500` للمرّة الثانية |
| **بندٌ بقي أخضر فوق السطر الذي وُضع ليمنعه** — «غير مؤكد» للهاتف و«موثَّق» للشارة، وتعبيري عرف الثانية وحدها | كُتب التعبيرُ من الذاكرة لا من الحزمة | ⚠️ **فحصٌ لا يرى ما يحرسه أسوأ من لا فحص** — يُقرأ النصُّ الحقيقيُّ من `i18n.js` قبل كتابة أيّ تعبيرٍ نمطيّ عليه |
| **تعليقُ قالبٍ `${/* */''}` داخل تعبيرٍ ثلاثيّ** كسر `profile.js` كلَّه | حُوكيَ اصطلاحُ الملفّ في موضعٍ لا يقبله | ⚠️ **داخل `${…}` التعليقُ `/* */` عاديّ، لا `${…}` ثانٍ** — والقياسُ التقطه، لا القراءة |
| **سنٌّ لم تعضّ في `475`** — إعادةُ حارس `requireTier` وحدَه تركت البنودَ السلوكيّة كلَّها خضراء | **طبقتان، وكلُّ واحدةٍ تكفي القارئ وحدها** — فوجودُ الأولى يحجب غيابَ الثانية عن أيّ فحصٍ سلوكيّ (درسُ `485`) | ⚠️ **البنودُ البنيويّة تقف إلى جانب السلوكيّة لا بدلاً عنها** — وإلّا تعفّنت إحدى الطبقتين والشبكةُ خضراء |
| **«إرجاعٌ» لم يُرجِع شيئاً** في السنّ الأولى لـ`475`: استُبدل سطرٌ من `tier()` وبقي شرطُ `tier2By` تحته | مُسّ نصفُ الدالّة وحُسِب أنّه كلُّها | ⚠️ **يُبرهَن وقوعُ الكسر قبل الحكم بأنّ الفحص نائم** — درسُ `500` نفسُه، للمرّة الثانية |
| **عُدِّل `CLAUDE.md` بعد انطلاق الشبكة** وسبعةُ سويتاتٍ تقرؤه | ظُنَّ أنّ الوثائق خارج ما تقرؤه السويتات | ⚠️ **التشغيلةُ تملك الشجرة** — تُوقَف وتُعاد من أوّلها، وخُضرةٌ فوق شجرةٍ تحرّكت أقلُّ قيمةً من لا تشغيلة |
| **قاعدةُ «الشبكة مرّةً لكلّ مجموعة» لم تُكسَر — أُفرِغت**: خمسُ كومِتاتِ إغلاقٍ متتاليةٍ (`605`·`600`·`575`·`470`·`475`) شغّلت الشبكةَ كاملةً، وكلٌّ منها داخلَ نصّ القاعدة لأنّها **أعلنت نفسَها مجموعةً من ملفٍّ واحد** | القاعدةُ تُستوفى بأن يسمّي المرءُ دفعتَه مجموعة، فصار الاستثناءُ هو الشكلَ الافتراضيّ | ⚠️ **قاعدةٌ يُشترَك فيها بالتسمية تدفع بالضبط ما كُتبت لتوفيره** — «مجموعةٌ من واحد» استثناءٌ يحتاج سببَه في رأس الملفّ، والسببُ الوحيدُ المقبول أن تمسّ الدفعةُ `js/store.js` أو الإقلاعَ أو المصادقة |
| **قلتُ إنّ المدّةَ لا تُشتقّ بأداةٍ كما يُشتقّ عددُ السويتات** | حُكِم على الاشتقاق بأنّه متعذّرٌ بلا محاولة | ⚠️ **وهي تُشتقّ**: عدّادٌ حول الحلقة في `run.sh` — و«يتعذّر» تُقال بعد القياس لا قبلَه |
| **فاتني موضعٌ سابعٌ للمدّة في الجلسة التي كتبتُ فيها درسَ المواضع الستّة** — «fifty minutes» ملفوفاً على سطرين | بحثتُ عن **العبارة** و`grep` لا يعبر لفَّ الأسطر | ⚠️ **في ملفٍّ ملفوفٍ باليد يُبحَث بالكلمة على نصٍّ مسطَّح، لا بالعبارة على أسطر** |
| **تعليقُ HTML داخل قالبٍ نصّيّ، وفيه علاماتُ اقتباسٍ خلفيّة** — أنهت العلامةُ القالبَ فصار `123456` رقماً غيرَ متوقَّع، وسقط `auth.js` ومعه `app.js` بالاستيراد: **كلُّ شاشةٍ بيضاء** | كُتب التعليقُ بأسلوب الملفّ في موضعٍ لا يقبله — **وفي الجلسة نفسِها التي كتبتُ فيها درسَه** | ⚠️ **داخل قالبٍ نصّيّ لا تعليقَ أصلاً: النثرُ فوق الدالّة.** ثمّ كتبتُ `*/` حرفيّاً داخل تعليقٍ كتليٍّ فأغلقتُه مبكّراً — والقياسُ التقطهما، لا القراءة |
| **بندان في سويتي الجديد كانا يقيسان لا شيء**: تعبيرُ CSP `[^"']*` يقف عند أوّل `'self'`، فالملفّان طابقا كلمتين و«نجحت» المقارنة بين اقتطاعين | كُتب التعبيرُ بلا تجربةٍ على النصّ الحقيقيّ | ⚠️ **فحصٌ لا يقيس شيئاً أسوأ من أحمر** — يُطبَع ما التقطه التعبيرُ مرّةً قبل الاعتماد عليه |
| **بندٌ كان يقيس البروكسي ويسمّيه انحداراً**: `domcontentloaded` يعطي 13 ثانيةً هنا، كلُّها ورقةُ خطوطٍ معلّقةٌ تحجب تنفيذَ الوحدات | قِيس الزمنُ من الشبكة لا من التطبيق | ⚠️ **يُعزَل ما يُقاس**: الورقةُ تُرفَض صراحةً، والزمنُ يبدأ من `commit` |
| **سكربتُ الأسنان أفسد ملفّاً عند الاستعادة** — تحويرٌ يستبدل نصّاً بالفراغ، معكوساً، يُدخِله عند الموضع صفر | `replace("", x, 1)` يُدخِل في البداية | ⚠️ **لا يُستعمَل الفراغُ مرساةً في تحويرٍ عكسيّ** — نجا الملفُّ لأنّه مولَّد، ولو كان بيدٍ لضاع ترتيبُه بصمت |
| **عشرُ سويتاتٍ كانت تنهار لا تحمّر** بعد أن صار الحسابُ على خادم وخرجت بطاقةُ رمز التجربة من شاشة البريد | السويتاتُ تكمل تسجيلاً حقيقيّاً عبر الواجهة، ولا طريقَ للحاوية إلى الإنترنت | ⚠️ **يُجاب الطرفُ ببديلٍ ولا تُليَّن قاعدةُ التطبيق** — والبديلُ يرفض ما لا يعرفه بصوتٍ عالٍ، فالمتساهلُ أخضرُ والميزةُ مكسورة |
| **صحّحتُ مدّةَ الشبكة في موضعٍ واحدٍ من ستّة** — فصار `CLAUDE.md` يحمل «ساعةً وثلاثةَ أرباع» في نثرٍ يُمَرُّ عليه و`~50 min` في **جدول البوّابات، وهو ما يُقرأ فعلاً** | صُحّح ما وقعت عليه العينُ ولم يُبحَث عن بقيّة النسخ | ⚠️ **تصحيحُ نسخةٍ واحدةٍ من عدّة أسوأ من تركها كلِّها شائخة** — فالملفُّ يناقض نفسَه ومن يقرأ أوّلَ ما يقع عليه يعمل به. **يُبحَث عن كلّ المواضع أوّلاً ثمّ تُصحَّح معاً** — وكان الجدولُ والقائمةُ يعطيان `~50` و`~20` **لنفس السكربت** شهوراً. **والمدّةُ لا تُشتقّ بأداةٍ كما يُشتقّ عددُ السويتات، فتُكتب ومعها تاريخُها**، ورقمٌ بلا تاريخٍ يُقرأ كأنّه ثابت |
| **سطرُ حالةِ فرعٍ في ملفّ الذاكرة بطل بين كتابته ووضعه** | الملفّ كُتب و`495` على الفرع، ووُضع بعد إغلاقه ودمجه | ⚠️ **حالةُ الفروع تُكتب مؤرَّخةً ومقيسة، ولا تُقرأ إلّا مع تاريخها** · **والمقياسُ الحيُّ هو `git ls-remote origin main`، لا سطرٌ في وثيقة** |

---

## 6) ما نزل وأُغلِق

```
485   ردٌّ فيه تحويل لا يفتح برنامجاً        · على main · 108 تشغيلة · 54 سويتاً · 5,922 بنداً
490   اسمُ المدينة لا يُستبدَل بأقرب مركز     · على main · 110 · 55 · 5,964
510   المخترَع لا يظهر لأحدٍ افتراضيّاً       · على main · 112 · 56 · 6,010
515   بطاقةُ المشاركة تحمل العنوانَ القديم    · على main · شبكةٌ خضراء
495   رقمُ الدعم يملأ مكانه               · على main · 114 تشغيلة · 57 سويتاً · 6,056 بنداً
520   ذاكرةُ المشروع تصير ملفّاً لا جلسة    · على main · وثائق فقط · بلا شبكة
```

**والمقيسُ على `main` بتاريخ 1 سبتمبر — سُئل الخادمُ نفسُه:**

```
git ls-remote origin main   →  d69c3aa  «520 — ذاكرةُ المشروع تصير ملفّاً لا جلسة»
APP_VERSION = '0.8.3'   ·   showDemo = false   ·   robots.txt موجود   ·   og:url = https://arabna.app/
```

---

## 7) الناقص — وما ينتظر مَن

### ما ينقص فعلاً

```
robots.txt                  ← مقفولٌ بقرار المالك · يُفتَح يوم الإطلاق لا قبله
```

⚠️ **وبندٌ سقط من هنا لأنّه لم يكن موجوداً أصلاً:** كان مكتوباً في هذا الموضع
أنّ `main` لم يتحرّك وأنّ اسمَ النطاق لا يُنشَر. **وهو خطئي — انظر جدولَ الأخطاء،
السطرَ الأخير.** **والمنعُ مرفوع: `510` و`515` و`490` تعمل على `arabna.app` اليوم.**

### على كود

`500` ← `435` ← `167` ← `470` ← ~~`475`~~ ← ~~`610`~~ ← وباقي الطابور: التالي `455`.

### على مالك البرنامج

```
تأسيس Arabna LLC          ← ✓ قامت · 29 أغسطس 2026
EIN                       ← التالي · مجّانيّ من IRS
بنك وStripe وApple        ← باسم الشركة، بعد EIN
Operating Agreement       ← ولو بمالكٍ واحد
IP Assignment ونقلُ الملكيّة ← النطاق · الشيفرة · الشعار · GitHub · Vercel · Supabase
رقم D-U-N-S               بعد الكيان
حساب Apple Developer      99 دولاراً سنويّاً
Mac + Xcode
اسمٌ وعنوانٌ قانونيّان للإيصالات
Supabase Pro              25 دولاراً شهريّاً · والمجّانيّ يتوقّف بعد أسبوع خمول
SMTP خاصّ لـSupabase       المدمج يرسل لأعضاء المشروع فقط · رسالتان في الساعة
كلمة سرّ الإدارة            ⚠️ هو ينشئها — لا أنا
إرسال PDF المحامي         بعد وضع اسمه مكان [الاسم]
```

⚠️ **وموعدٌ مسجَّل:** مشروع Supabase المجّانيّ المتوقّف **يُحذَف نهائيّاً
بعد 90 يوماً** — أي ≈ نهاية نوفمبر 2026.

### مؤجَّلٌ بقرار

```
Geocoding خلف Proxy: إن احتيج مزوّدٌ ثانٍ يوماً — Proxy على السيرفر بحدّ سرعةٍ وcache وkill switch، أو مزوّدٌ تجاريّ · لا نداءَ مباشراً من الجهاز إلى خادمٍ عامٍّ له سياسة
حراسُ رخص TDLR والطعام المنزليّ    ينتظر المحامي
«لا شيء مؤقّتٌ يبقى ظاهراً»
عطلُ eventFree
«صيانة وخدمات المنزل» التاسعة عشرة من 22 في المنتقي
بندُ test_v53 · 6.4 لا يمكن أن يحمرّ على البناء المفرد (|| true وترميز base64)
```

---

## 8) متطلّبات App Store — الحال المقيس

```
حذفُ الحساب داخل البرنامج        نازل · deleteAccount + purgeAccount · حذفٌ حقيقيّ
حظرٌ فوريٌّ بلا مراجعة              نازل · blockUser · unblockUser · isBlocked
التبليغ عن محتوى                نازل · addFlag + طابور إشراف
تصديرُ البيانات                  نازل · exportMyData
سياسةُ خصوصيّةٍ وشروط              نازل · شاشتان: #/privacy و#/terms
وسيلةُ تواصل                     support@arabna.app · وأُكِّد أنّه يستقبل (1 سبتمبر)
أهدافُ اللمس                     44 نقطةً حدّاً أدنى — مقيسةٌ في الشريط السفليّ والترويسة (570) · ويُعاد قياسُها لأيّ صفٍّ جديد
الغلافُ الأصليّ — Guideline 4.2   ⚠️ ناقص · ولا جواب مستقرّ له
E-08 من وثيقة النقل: Nominatim معطَّلة في الإنتاج (550) · لا تُعاد إلّا مع السيرفر: حدُّ سرعةٍ لإجمالي التطبيق · cache · kill switch · Referer/User-Agent · نسبة · وإعادة اختبار موثَّقة — والحكم للمراجعة القانونيّة
```

⚠️ **و`4.2` مفتوح:** الوظيفةُ الوحيدة الحصريّةُ للغلاف اليوم **جدولةُ
تنبيهٍ محلّيٍّ لوقتٍ قادم** — والدليلُ بلا إنترنت ومواقيتُ الصلاة
**يعملان على الويب عندنا أصلاً**. **وقرارُه يخصّ دفعة الغلاف.**

**وقيودٌ خارجيّة تُقال لئلّا تُفتَح من جديد:**

```
زرُّ «ثبّته بضغطة» على iOS مستحيل — لا واجهة تثبيتٍ برمجيّة
Notification Triggers أُوقِف تطويرها — فلا جدولةَ تنبيهٍ على الويب
حدُّ iOS 64 تنبيهاً معلَّقاً — أي ثلاثةَ عشرَ يوماً ثمّ صمت
Safari تحذف تخزينَ التبويب بعد سبعة أيّامٍ بلا تفاعل — والمضافُ للشاشة مستثنى
```

---

## 9) وعودٌ قطعتُها ولم تُنفَّذ بعد

```
دفعةٌ صغيرة: وصفُ مكان البانر المصغّر في «أعلن معنا» خاطئ — الملفّ 505 كُتب وينتظر دوره
تعبئةُ mailto سلفاً (الموضوع والتصنيف والنسخة والشاشة) — مؤجَّلة لدفعة السيرفر بقرار المالك
سجلُّ القرارات في اللوحة يُحدَّث بعد كلّ قرارٍ بلا أن يُطلَب
هذا الملفّ يُحدَّث بعد كلّ مهمّة
```

---

## 10) سجلُّ التحديثات على هذا الملفّ

```
1 سبتمبر 2026   أُنشئ بأمر مالك البرنامج · وعُبّئ من الحالة القائمة والمحادثات
1 سبتمبر 2026   أُضيفت تواريخُ المشروع المقيسة إلى القسم 1 (أوّلُ كومِت 15 أغسطس)
1 سبتمبر 2026   صُحِّح القسمان 6 و7: main على 1799b4f (0.8.2) و490 و510 و515 عليه
                · ورُفع المنعُ عن نشر اسم النطاق · وأُضيف الخطأُ إلى جدول الأخطاء
1 سبتمبر 2026   أُغلِق 495 و520 ونزلا على main · وصُحِّح القسمان 6 و7 بالقياس
2 سبتمبر 2026   550 — Nominatim معطَّلة: الشيفرة والـCSP وsw.js · مزوّدٌ واحد · بقرار وثيقة النقل E-08
1 سبتمبر 2026   قامت Arabna LLC (29 أغسطس) · وانقلب قرارُ «باسم المالك» إلى «باسم الشركة»
2 سبتمبر 2026   ردُّ المحامي — الجولة الثانية: سبعةُ أجوبةٍ وأربعُ تصحيحات · و545 و344 يحملان ما يخصّ المستودع
2 سبتمبر 2026   555 — ثلاثةُ نصوصٍ كانت تبيع التقييمات بالاشتراك صُحّحت على القرار والشيفرة · ومفتاحان ميّتان بالرقم 10 حُذفا
2 سبتمبر 2026   560 — بطاقةُ الترقية في الدليل صارت تُرى: صنفٌ واحد وحركتان هادئتان · directory.js و app.css
3 سبتمبر 2026   375 — اسمُ مالك البرنامج خرج من المستودع العلنيّ · ورسائلُ كوميتات اليوم أُعيدت كتابتها بلا قيمةٍ حقيقيّة
3 سبتمبر 2026   565 — عشرُ صورٍ للمشترك رقماً في مكانٍ واحد · وجملةُ «سجّل أوّلاً» شيلت من بطاقات الترقية
3 سبتمبر 2026   570 — ثلاثةٌ من الفحص الشامل: 44 بكسلاً في الشريط · حارسٌ يرى الداخليّ · وتسعةُ إعادات توجيهٍ صارت تقول لماذا
4 سبتمبر 2026   571 — العاشر الذي فات التسعة صار يقول لماذا · وحادي عشرَ من الصنف نفسِه (ReceiptScreen) سُجّل ولم يُصلَح
4 سبتمبر 2026   572 — الحادي عشر صار يقول لماذا · والعائلةُ صارت مقيسةً مقفلة (15 حارساً: 11 يتكلّم · 3 صلاحيّة · 1 عمليّةٌ فشلت)
4 سبتمبر 2026   ⚠️ مسجَّلٌ لا مُصلَح: تمييزُ الإعلان يشحن البطاقة قبل أن يتحقّق من نجاح التمييز، فيرجع بلا إيصالٍ وبلا كلمة — يخصّ الدفع
4 سبتمبر 2026   575 — بطاقةُ الترقية في الدليل تتكرّر كلّ عشر نتائج · directory.js
4 سبتمبر 2026   600 — ساعاتُ العطل تصريحٌ من صاحب النشاط لا تخمين · holidays.js جديد · والحالةُ الثالثة «لم يُجَب» ليست «لا»
4 سبتمبر 2026   470 — عقدُ السيرفر: مخطّطٌ وقواعدُ حماية في supabase/ وحده · بلا وصلٍ ولا مفتاح ولا سطرٍ في js/
4 سبتمبر 2026   605 — عنوانُ المقال كان يلبس لافتة السوق: مفتاحٌ مشتركٌ استُعير خطأً · admin.js وحده · خارج ترتيب الطابور بقرار مالك البرنامج
5 سبتمبر 2026   475 — توثيقُ الجوال يُطفَأ بمفتاح ولا يُحذَف · والدرجةُ الثانية تُنال بالبريد · tier2By يمنع نزولَ من نالها يوم يُقلَب المفتاح
5 سبتمبر 2026   475 (توسيع) — الموضعُ الرابع · وتأكيدُ تغيير الرقم ينتقل إلى البريد: يؤكّد التغيير ولا يوثّق الرقم
5 سبتمبر 2026   قرارُ مالك البرنامج: الشبكةُ الكاملة مرّةً في نهاية المجموعة لا بعد كلّ دفعة — يسري من الدفعة التالية
5 سبتمبر 2026   وقياسُ زمن كلّ سويتٍ يُشتقّ في `run.sh` — ملفٌّ مرقَّم (`615`) لا يُسبَق
5 سبتمبر 2026   610 — أوّلُ سطرٍ يتّصل بسوبابيس: الهويّةُ وحدَها، والمكتبةُ مودَعةٌ لا من CDN
5 سبتمبر 2026   قرارُ مالك البرنامج: `js/vendor/` لا esm.sh — وعدُ 420 يسبق نصَّ المواصفة
6 سبتمبر 2026   قرارُ مالك البرنامج: الشبكةُ تُشغَّل على مقاطعَ داخل نوافذَ صاحية — الحاويةُ تُعلَّق فتكذب ساعةُ الحائط
6 سبتمبر 2026   وثلاثةُ شروطٍ تجعلها دليلاً واحداً: كلُّ السويتات · شجرةٌ واحدةٌ يُتحقّق منها عند كلّ مقطع · ولا نتيجةَ مستعارة
6 سبتمبر 2026   610 أُغلِق: 146 تشغيلة · 73 سويتاً · 6821 بنداً · صفر أحمر · صفر انهيار — على تسعة عشر مقطعاً فوق d69603f
6 سبتمبر 2026   الطابور كان ساكتاً عن 610 و615 و620 — كُتبت الثلاثةُ بعد آخر كتابةٍ له، فأُضيفت في كومِت الإغلاق
6 سبتمبر 2026   620 — الخادمُ يقرّر مَن صاحب الحساب: كلمةُ المرور تصل إليه · تغييرُ البريد يطلبها · الإرسالُ حقيقيّ · الاستعادةُ تعمل · قسمُ المستخدمين بقفلين
6 سبتمبر 2026   قرارُ مالك البرنامج: بريدُ المستخدم يُعرَض كاملاً في لوحة الإدارة — التقنيعُ نوقش ورُفض
6 سبتمبر 2026   ⚠️ نوعُ رمز البريد يُختار من حال الحساب: resend/signup على عنوانٍ مؤكَّدٍ يرفضه الخادم — طريقُ 475 كان مكسوراً وهو يبدو سليماً
6 سبتمبر 2026   ⚠️ وتصحيح: profiles تحمل سياسةَ قراءةٍ للأدمن أصلاً — الفجوةُ أنّها لا تحمل عمودَ بريد، ولا يُضاف
6 سبتمبر 2026   قاعدة: الفحصُ يقرأ الشيفرة لا النثرَ عنها — وامتدّت إلى SQL بعد أن حمّرت على تعليقٍ يشرح قاعدتَه
6 سبتمبر 2026   قاعدة: يُبرهَن أنّ كسرَ السنّ وقع حيث صُوِّب، لا أنّ أحمرَ ظهر — طفرةٌ أصابت أوّلَ ظهورٍ لسطرٍ مكرَّر
6 سبتمبر 2026   620 (ملحق ١) — autocomplete=off كان على كلّ حقول كلمة المرور: نقصُ أمانٍ لا راحة · والوصفُ صار وسيطاً · وone-time-code على الخانة الأولى وحدَها
6 سبتمبر 2026   ⚠️ وثلاثةَ عشرَ حقلاً لا ستّة — اثنان منها أضافتهما الدفعةُ نفسُها: خريطةٌ للستّة كانت ستترك أحدثَ شاشةٍ على العطل
6 سبتمبر 2026   620 (ملحق ٢) — 0001 وعد بمشغِّلٍ ينشئ صفَّ profiles ولم يُكتَب: مقيسٌ حيّاً users=1 profiles=0 · نُفِّذ باليد و0006 يكتبه حرفاً بحرف
6 سبتمبر 2026   قاعدة: ملفُّ هجرةٍ يخالف القاعدةَ في حرفٍ أسوأُ من ملفٍّ غير موجود، لأنّه يُصدَّق · ورقمُ الهجرة مفتاحٌ لا يُعاد ترقيمُه
6 سبتمبر 2026   display_name صار يصل الخادم — كان يُكتَب مرّةً عند التسجيل، فأوّلُ فتحةٍ على جهازٍ ثانٍ تُرجِع الاسمَ القديم
6 سبتمبر 2026   625 — الحقلُ الذي بنته 620 كان الوحيد خارج passwordField · وبندٌ بنيويّ: لا input type=password خامٌ في js/
6 سبتمبر 2026   ⚠️ قاعدة: القاعدةُ لا تُطبَّق على ما وُجد وحدَه بل على ما يُكتَب في اللحظة نفسِها — دفعةٌ صحّحت 13 موضعاً ثمّ أنشأت الرابعَ عشرَ خارجَها
6 سبتمبر 2026   ⚠️ Supabase لا يقول إنّ عنواناً مأخوذٌ بقصد — فيُرجِع نجاحاً ولا يُرسل، ولا قراءةَ خطأٍ تُصلح ذلك، الجملةُ وحدَها تُصلحه
6 سبتمبر 2026   ⚠️ supabase-js لا يرمي بل يُرجِع { data, error } — كلُّ try/catch حوله يبتلع الفشلَ صامتاً
6 سبتمبر 2026   ⚠️ العطلان وجدهما مالكُ البرنامج على المضيف الحيّ لا الشبكة: دفعةٌ خضراءُ بالكامل تشحن عطلاً يصيب أوّلَ من يمرّ
6 سبتمبر 2026   630 — الإعلاناتُ تُقرأ من الخادم · المرشّحُ لـRLS · الموافقةُ تصل الخادمَ أوّلاً · 0007 admin: insert · قفلُ اللوحة يُشال والحسابُ هو القفل (يلغي 620) · والسعرُ صار يصل رقماً
```
