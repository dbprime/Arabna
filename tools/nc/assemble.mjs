import fs from 'fs';
const head = fs.readFileSync(new URL('./nc-head.md', import.meta.url),'utf8');
const body = fs.readFileSync(new URL('./nc-body.txt', import.meta.url),'utf8');

const module = `/* ============================================================
   ARABNA — دليل الواصل الجديد: المحتوى
   ------------------------------------------------------------
   سبعة عشر قسماً، بالعربيّة والإنجليزيّة، أقرّها رامي كلمةً كلمة.

   ولا HTML هنا. المفردات ثمانٍ لا تاسعة لها:
     ['p', نصّ] ['h', نصّ] ['warn', نصّ] ['alert', نصّ] ['do', نصّ]
     ['ul', نصّ, نصّ, …]   ['rows', [يمين, يسار], …]
     ['tool', 'flood', نصّ يحلّ محلّها إن لم تعمل]
   و do سطرٌ يقول ماذا يكتب المستخدم وأيّ جوابٍ ينتظره — تحت كلّ رابط بحث.
   وداخل النصّ ثلاث علامات: **عريض** · [ظاهر](رابط) · \`لاتينيّ\`
   والراسم في newcomer.js يهرب أوّلاً ثمّ يطبّقها — ولا عكس.

   و cat هي فئة الدليل التي يُختار منها النشاط ذو الصلة آخر القسم،
   بالأولويّة: مشترك ← مميّز ← مجّانيّ. ولا يُكتب اسم نشاطٍ هنا أبداً:
   المحلّ يغلق والنصّ يبقى.
   ============================================================ */

export const NC_SECTIONS = [
${body.trim()}
];
`;

const out = head +
  '```js\n' + module + '```\n' + `

---

# الاختبار

1. **افتح \`#/newcomer\`** ← **سبعة عشر قسماً**، لا ثمانية، **ولا كلمة
   «قيد الإعداد» في الشاشة كلّها.**
2. **افتح كلّ قسم** ← **لا وسم \`HTML\` مطبوع** في أيّ فقرة، بأيّ لغة.
   (وهذا اختبار شريط المموّل نفسه، في الشاشة الثانية.)
3. **اضغط رقم هاتف** ← يفتح الاتّصال، **والرقم يُقرأ من اليسار** داخل
   الفقرة العربيّة.
4. **اضغط رابطاً** ← يفتح خارج البرنامج، **ويرجع البرنامج كما تركته**.
5. **زرّ اللغة** ← يبدّل الدليل وحده، **والقائمة الجانبيّة والرئيسيّة
   تبقيان على لغة البرنامج.**
6. **بدّل اللغة وأغلق البرنامج وافتحه** ← **الدليل على اللغة التي
   اخترتها**، لا على لغة البرنامج.
7. **الإنجليزيّة** ← الصفحة \`ltr\`، **و\`<html>\` لم يتغيّر اتّجاهه.**
8. **آخر كلّ قسم** ← نشاط واحد، **ومشتركٌ إن وُجد**، ثمّ مميّز، ثمّ
   مجّانيّ بلا شارة.
9. **أفرغ فئة \`lawyers\` من الدليل** ← **قسم الهجرة ينتهي بلا مساحة
   إعلانيّة أصلاً**، ولا إطار فارغ.
10. **زرّ التبليغ** ← ورقة بأسباب «ما ننزله نحن»، **لا أسباب السوق**.
11. **الوضعان** ← الغامق والفاتح، **وصناديق التحذير مقروءة في الاثنين**.
12. **عُدّ الروابط** ← **86 رابطاً و41 هاتفاً في كلّ لغة**، والعدد
    نفسه في الاثنتين. (وهذا يُقاس آليّاً، لا بالعين.)
13. **ابحث عن رقم عربيّ-هنديّ (٠١٢…) في الملفّ كلّه** ← **صفر.**
14. **ابحث عن «كانون» و«تمّوز» و«آذار» و«تشرين»** ← **صفر**، والأشهر
    كلّها بالأسماء الأجنبيّة.
15. صفر مكتبة جديدة · صفر خطأ console. **وطلبا الشبكة الوحيدان هما طلبا
    أداة الفيضان، ولا يخرجان إلّا حين يضغط المستخدم «افحص».**
16. **عُدّ أسطر \`do\`** ← **25 سطراً في كلّ لغة**، وتحت كلّ رابطٍ يُطلب
    فيه بحث. **ولا سطر \`do\` بلا رابطٍ فوقه.**
17. **أداة الفيضان** ← **مرّة واحدة في الدليل كلّه**، في القسم الثاني،
    **وسطر «لا يُخزَّن» تحت الخانة قبل الكتابة لا بعدها.**
18. **أغلق الشبكة واضغط «افحص»** ← **تختفي الخانة ويظهر مكانها سطر
    \`FEMA\`** — **ولا رسالة خطأ، ولا خانةٌ تبتلع ما يُكتب فيها ولا تردّ.**
19. **اكتب عنواناً لا وجود له** ← **«ما وصلنا جواباً لهذا العنوان»** —
    **ولا يُقال «خارج المنطقة» أبداً لعنوانٍ لم يُعرَف.**
20. **افتح الدليل ثمّ أغلق البرنامج وافتحه** ← **الخانة فارغة**، ولا أثر
    لآخر عنوانٍ بُحث فيه، لا في \`arabna.v1\` ولا في أيّ مكان.
`;

fs.writeFileSync(new URL('./دليل-الواصل-النص-الكامل.md', import.meta.url), out);
fs.writeFileSync(new URL('../../js/newcomer-content.js', import.meta.url), module);
console.log('md ' + Math.round(out.length/1024) + 'KB · module ' + Math.round(module.length/1024) + 'KB');
