# عربنا — الحالة

آخر تحديث: 2026-09-01 · **V.08.2**

> **الرقم يُرفَع مرّةً واحدة للدفعة، لا مرّةً لكلّ ملفّ.**
> V.04.4 = `170` و`175` · **V.04.5 = `015` و`025`.**
> و`180`/`185` **لم يرفعاه**: أدواتٌ ووثائق، ولا سطر في `js/` ولا `styles/`.
> **V.04.6 = `190`** — التحديث الصامت. · **V.04.7 = `020`** — دفعة 9ج.
> **V.04.8 = `195`** — تفضيلات الجهاز. · **V.04.9 = `200`** — وصل المساجد وترتيب الكتلتين.
> و`210` **لم يرفعه**: وثائق. · **V.05.0 = `220`** — ألوان الأقسام. · **V.05.1 = `205`** — الكلمة الدوّارة وشعار الدرج.
> **V.05.2 = `225`** — الخروج يُنهي الملكيّة. · **V.05.3 = `230`** — حبر السوق والكلمة الدوّارة والشعار.
> و`235` **لم يرفعه**: أداة اختبار. · **V.05.4 = `240`** — الملكيّة صارت جمعاً.
> و`245`/`270` **لم يرفعاه**: إصلاحُ سويتات، والبناء صحيح فيها كلّها.
> **V.05.5 = `255`** — الدرج والمركز وصور العرض. · **V.05.6 = `260`** — أيقونات التواصل.
> **V.05.7 = `265`** — البريد برمز، وصفة الأعمال، وأسماء المنتجات.
> **V.05.8 = `280`** — «قريباً» تُقرأ ولا تُلمَس. · **V.05.9 = `275`** — العلم يصل كاملاً.
> **V.06.0 = `285`** — المظهر يبدأ من الجهاز في كلّ فتحة.
> **V.06.1 = `295`** — «من نحن» بكلمات رامي، وعلامة X الحقيقيّة.
> **V.06.2 = `305`** — بلاطات الدرج، والمسجد، والصفّ المكرّر يخرج.
> **V.06.3 = `310`** — القائمة تقول كلّ الشروط، والرفض يُقرأ رفضاً.
> **V.06.4 = `307`** — ما نجا من `esc()` يُنفَّذ، والرفض بلا سبب.
> **V.06.5 = `308`** — الموجة الثانية: `ui.js` و`directory.js` وأربعةٌ غيرها.
> **V.06.6 = `315`** — قسم «حسابي»: خروجٌ لا يمسح شيئاً، وأربعةَ عشرَ بنداً.
> **V.06.7 = `325`** — لوحة «حسابي» تحكي حالتها، و«طلباتي» تُقرأ أخيراً.
> **V.06.8 = `326`** — الرقم المعلَّق، ونسخة القارئ من بياناته.
> ⚠️ **ومجموعة «حسابي» (`315` + `325` + `326`) أُغلقت خضراء** — الشبكة الكاملة
> بعد `326` على النسختين: **92 تشغيلة · 46 سويتاً · 5,456 بنداً · صفر أحمر.**
> **V.06.9 = `330`** — الدليل: من دفع فوق، والأقرب أوّلاً.
> ⚠️ **و`330` لا يقفل مجموعته** — المجموعة `330` ثمّ `335`، **و`335` هو القافل**،
> فالشبكة الكاملة تُشغَّل معه.
> **V.08.2 = `515`** — بطاقةُ المشاركة تحمل العنوانَ الجديد. **ولا يقفل
> مجموعةً** — الطابور بعده `495`.
> ⚠️ **رُبط `arabna.app` في 1 سبتمبر، وثلاثةُ عناوين مطلقة في
> `index.html` بقيت مكتوبةً بالحرف على المضيف المؤقّت** — و`og:url` هو ما
> يُعلنه البرنامج عنواناً له، **فمن يشارك الرابط تُبنى بطاقتُه على عنوانٍ
> سيُهجَر، ومن يضغطها ينزل هناك لا على `arabna.app`.** صارت الثلاثةُ على
> النطاق، **وتبقى مطلقةً**: زاحفُ فيسبوك وواتساب لا يقرأ نسبيّاً، والعطلُ
> كان في المضيف لا في الشكل.
> ⚠️ **والعطلُ الأعمق أنّ لا شيء كان ينتبه** — ثلاثةُ عناوين نامت أسابيع.
> فبندٌ دائمٌ في `test_v59`: **لا عنوانَ مطلقٍ على مضيف `vercel.app` في
> أيّ ملفٍّ منشور** — شرطُ نفيٍ على المضيف لا على الاسم القديم، وهي قاعدة
> `390`.
> ⚠️ **ونصٌّ مرسومٌ داخل صورةٍ لا تصله دفعة**: البطاقةُ كانت تقول «في
> Houston» والبرنامجُ على شاشته يقول «في أمريكا» — تحدّث نصُّ الشاشة يوم
> اتّسع المدى وبقي نصُّ الصورة. **وبقرار رامي: السطر يُشال ويبقى الشعار
> وحدَه** — والعنوانُ والوصفُ يظهران تحت الصورة في واتساب وفيسبوك معاً،
> فكان السطرُ تكراراً. **والملفُّ وصل جاهزاً من رامي ووُضع كما هو؛ لا
> رسمَ ولا توليدَ نصّ.** والفحصُ مشتقٌّ: **كتلتان قبلُ، وواحدةٌ بعد.**
> ⚠️ **ورقمُ النسخة رُفع لسببٍ مقيس**: عاملُ الخدمة يخزّن `index.html`
> في مخزنٍ اسمُه من الرقم، **فبغير رفعٍ يبقى المثبِّتُ على رأسٍ قديمٍ
> وبطاقةٍ قديمة.** **ولا رقمَ حرفيّاً في الفحص** — يُقاس أنّ الحاملات
> تتّفق: `js/data.js` و`js/sw-manifest.js` وسطرُ `CLAUDE.md`.
> ⚠️ **و`test_v56` كان يبني مضيفَه على منفذٍ مكتوب (8451) و`run.sh`
> يشغّل البناءين معاً** — فماتت التشغيلةُ الثانية بـ`EADDRINUSE`،
> **انهياراً لا سقوطاً**، ونجَت من شبكةٍ كاملةٍ واحدةٍ لأنّ التشغيلتين
> تباعدتا. المنفذُ `0` الآن: النواةُ تختاره ولا رقمَ يتصادم. **من عائلة
> منفذ `test_v36` المكتوب.**
> ⚠️ **ولم يُبنَ تحويلٌ من العنوان المؤقّت، و`robots.txt` باقٍ مقفولاً** —
> الاثنان في دفعة النشر.
> **V.08.1 = `510`** — المخترَع لا يظهر لأحدٍ افتراضيّاً. **بوّابةُ نشر،
> وأوّلُ ما أُرسِل.** ولا يقفل مجموعةً — الطابور بعده `495`.
> ⚠️ **والعطل لم يكن في البيانات بل فيمن يراها**: `showDemo` تفضيلُ
> **جهاز** لا إعدادُ برنامج — تُحفَظ في مخزن الهاتف نفسِه — فإطفاؤها على
> جهاز رامي أخفاها على جهازه وحدَه، وكلُّ زائرٍ يبدأ من الافتراضيّ.
> **وما دام الافتراضيُّ `true` لم تكن هناك طريقةٌ تُخفيها عن الناس، ولا
> واحدة.** مقيسٌ على جهازٍ جديد بعد الإصلاح: شريحةٌ واحدة · السوق 0 ·
> المجلّة 0 · **والدليل 40 صفّاً كما هو**.
> ⚠️ **ونصّان في اللوحة كانا يقولان غير الحقيقة**: «لا يراها أحد» تصف
> سيرفراً لا وجود له، و«ظاهرة للمستخدمين» تُعرَض بحالة هذا الجهاز — **فكان
> إطفاءُ المفتاح يُسكِت الإنذار ولا يصلح شيئاً**. صارا يذكران «هذا الجهاز».
> ⚠️ **والتقييماتُ المخترَعة تبقى خلف المفتاح بقرار رامي** — وحذفُها من
> `js/data.js` بندُ إطلاقٍ مكتوبٌ في القسم 1.ب أعلاه.
> ⚠️ **و38 سويتاً احمرّت لأنّ فِخاخَها هي السجلّات المخترَعة نفسُها** —
> لا لأنّ شيئاً انكسر. `tools/e2e/_demo.mjs` غلافٌ واحد يُشعل المفتاح
> للسويتات وحدها. وفيه خطآن قِيسا ولم يُستنتَجا: **أشعله دون شرط**
> فداس اختيار `v20 · 6.7` الصريح بإطفائه — مساعدةٌ تكسر فحصاً —
> فصار لا يمسّ كائناً يحمل `showDemo` أصلاً؛ و**`addInitScript` يعمل
> بترتيب التسجيل**، وخمسُ سويتات (`v38` · `v39` · `v40` · `v43` · `v45`)
> تبذر باستبدال الحالة كلِّها فتمحو الوسمَين بعد لحظة — **فصار الاعتراضُ
> عند الكتابة**، وهو الموضع الوحيد المحصَّن من الترتيب.
> ⚠️ **والشبكة الكاملة خضراء على البناءين: 112 تشغيلة · 56 سويتاً ·
> 6,010 بنداً · صفر أحمر · صفر انهيار.**
> **V.08.0 = `490`** — اسمُ المدينة لا يُستبدَل بأقرب مركز. **ولا يقفل
> مجموعةً** — الطابور بعده `495`.
> ⚠️ **بلاغُ رامي: الشاشة تقول Sugar Land وهو ليس فيها.** والعطل نصفُ
> إصلاحٍ قديم: `cityNameFor` ترمي جوابَ البحث العكسيّ إن كانت المدينةُ
> خارج الأربع والعشرين. مقيس: **Rosenberg → Sugar Land**، وصارت
> **Rosenberg**. والشرطُ لم يكن يختار بين اسمين — كان يختار متى يُرمى
> الصحيح.
> ⚠️ **ونقضُ قرار V.04.0 بأمر رامي**: المدينة المختارة باليد لم تعد
> تُقفَل — تُحدَّث تلقائيّاً فوق ثلاثة أميال، وسطرٌ عابرٌ فيه **زرُّ تراجعٍ
> واحد** يعيد المدينة ووسمَها. واللوحُ ودالّتاه ومفاتيحُ `locMoved*`
> محذوفةٌ حذفاً: **صفرُ مواضع في `js/`**.
> ⚠️ **وثلاث سويتات احمرّت أوّلاً**: `v24 · 1.2` و`1.3` انقلابٌ مقصود ·
> `v33 · 2.6` **انهارت** بنداءِ دالّةٍ محذوفة · و`v56 · 3.6` **خطأٌ في
> فحصٍ كُتب في `485`**: رقمُ نسخةٍ مجمَّدٌ حرفيّاً، فاحمرّ في الدفعة
> التالية ولا شيء معطوب. **رقمٌ مجمَّدٌ في فحصٍ حمرةٌ مجدولةٌ لتاريخٍ قادم.**
> ⚠️ **والشبكة الكاملة خضراء على البناءين: 110 تشغيلة · 55 سويتاً ·
> 5,964 بنداً · صفر أحمر · صفر انهيار.**
> **V.07.9 = `485`** — ردٌّ فيه تحويل لا يفتح برنامجاً. **ولا يقفل مجموعةً**
> — الطابور بعده `435`.
> ⚠️ **عطلٌ قاتلٌ كان على المنشور:** البرنامج المضاف إلى الشاشة على iPhone
> **لا يفتح أصلاً** — «Response served by service worker has redirections».
> صنعتْه ثلاثةُ أسطرٍ معاً: `start_url = './index.html#/home'` ·
> `"cleanUrls": true` (فـ`/index.html` يردّ 308) · وعاملُ الخدمة يخزّن
> `index.html` ويستعملها لكلّ تنقّل. مقيسٌ بخادمٍ بُني ليحوّل، وقُرئ المخزن:
> **الصفّ `/index.html -> /` عليه `redirected: true`، والتنقّل الثاني
> ERR_FAILED.** بعد الإصلاح: **صفوفٌ مسمومة 0 · والتنقّل الثاني 1,138 حرفاً.**
> ⚠️ **ولا تكشفه تجربةٌ واحدة**: الزيارةُ الأولى تمرّ من الشبكة دائماً.
> ⚠️ **والشبكة الكاملة خضراء على البناءين: 108 تشغيلة · 54 سويتاً ·
> 5,922 بنداً · صفر أحمر · صفر انهيار.**
> **V.07.8 = `480`** — شريطٌ مخفيٌّ يرسم نفسه ويبتلع اللمسات. **ولا يقفل
> مجموعةً** — الطابور بعده `435`.
> ⚠️ **والشبكة الكاملة خضراء على البناءين: 106 تشغيلة · 53 سويتاً ·
> 5,894 بنداً · صفر أحمر · صفر انهيار.** ولا بندٌ سقط أوّلاً.
> ⚠️ **عطلٌ من `425` كان على المنشور، ورآه رامي على جهازه:** `[hidden]`
> يأتي من ورقة المتصفّح بأضعف أولويّة، فألغته `display: flex` في
> `.install-bar`. مقيسٌ على عشر شاشات: **374 × 22 عند y=736**،
> و`elementFromPoint` في وسطه يعطي الشريط نفسه — **فوق آخر سجلٍّ ظاهرٍ في
> الدليل، يأخذ اللمسة ولا يفعل بها شيئاً.**
> ⚠️ **والأهمّ أنّ الشبكة كانت خضراء فوقه:** `barShown` كان يقرأ الخاصّيّة
> لا البكسل، والخاصّيّة صحيحة. أُعيدت كتابته، **وأُضيف بندٌ عامٌّ لا باسم
> عنصر** — بندٌ باسم عنصرٍ يحرس عنصراً، وهذا **صنفٌ لا حادثة**.
> ⚠️ **و`.search-clear[hidden]` حُذف**: ترقيعٌ موضعيٌّ للصنف نفسه، وقع من
> قبل وعُولِج في مكانه وحده. `[hidden]` في الورقة **2 → 1**.
> **V.07.7 = `430`** — البرنامج يستعدّ للغلاف الأصليّ، والبيانات تنجو أوّلاً.
> **ولا يقفل مجموعةً** — الطابور بعده `435` ثمّ `167`.
> ⚠️ **والشبكة الكاملة خضراء على البناءين: 106 تشغيلة · 53 سويتاً ·
> 5,888 بنداً · صفر أحمر · صفر انهيار.**
> ⚠️ **وحمراوان سقطتا أوّلاً، وكلتاهما من يد الجلسة لا عطبٌ في البرنامج:**
> `v33 · 6.2` **انقلابٌ مقصود** — كان يؤكّد أنّ مفتاح الأذان يلوم السيرفر،
> وأُعيدت كتابته ثلاثةَ بنودٍ بتعليقٍ يسمّي الانقلاب؛ و`v54 · 8.6` و`8.7`
> **حمرةٌ كاذبة** — `jsAll` كان يقرأ المصدر الخام، فطابق تعليقَ
> `prayer.js` الذي يشرح أنّ تلك الواجهات غير موجودة. أُصلح بسطر `test_v53`
> بحرفه. **ولا واحدٌ منهما لُيِّن، ولا التعليق حُذف.**
> ⚠️ **وليس هو الغلاف**: الغلاف يحتاج Mac وXcode وحساب Apple. وكلّ بندٍ فيه
> ينفع القارئ على الويب اليوم.
> ⚠️ **والأرقام الثلاثة التي تُغلَق بها**: مواضع `localStorage` في `js/`
> **6 → 1** · `window.open(` **2 → 0** · وأكبرُ ناتجٍ لجدول التنبيهات **64**.
> ⚠️ **وصُحِّح نصٌّ كان يُنتظَر به المعلمُ الخطأ**: «تنبيه قبل الأذان» كان
> يقول «يعمل مع إطلاق السيرفر» — والمواقيت تُحسَب على الجهاز بلا طلبِ شبكةٍ
> واحد، والتنبيه المحلّيّ يُجدوَل على الجهاز. **فلا سيرفر فيه أصلاً**، وصار
> «يحتاج نسخةً أصليّة من البرنامج».
> **V.07.6 = `425`** — «أضِفه إلى شاشتك». **ولا يقفل مجموعةً** — الطابور بعده
> `430` ثمّ `435`.
> ⚠️ **والشبكة الكاملة خضراء على البناءين: 104 تشغيلة · 52 سويتاً ·
> 5,826 بنداً · صفر أحمر · صفر انهيار.** ولا سويت أُعيدت كتابتها.
> ⚠️ **والفحص اليدويّ الثلاثيّ لم يُجرَ ولا واحدٌ منه** — iPhone من Safari ·
> iPhone من رابط فيسبوك · Android. لا يُجرى من الحاوية، وينتظر هاتف رامي
> على معاينة الفرع.
> ⚠️ **وأخطر ما فيه ليس بنداً من الملفّ:** عدّادُ الفتحات كشف أنّ `boot()`
> **كان يعمل مرّتين منذ كُتب المسار** — سكربت الوحدات مؤجَّل، فـ`readyState`
> يتجاوز `'loading'` قبل تنفيذه، فيناديه سطرُ آخر الملفّ ثمّ يناديه
> `DOMContentLoaded`. وكلّ ما يفعله كان عديم الأثر بالتكرار فلم يبدُ شيء
> خطأ. حارسُ دخولٍ واحد، والمدخلان يبقيان.
> ⚠️ **وأربعة من فحوص الملفّ لا تجريها الشبكة**: Chromium لا يحاكي
> `display-mode` (مسجَّلٌ عندنا منذ V.01.7)، ولا يطلق `beforeinstallprompt`
> حقيقيّاً، ولا لوحةَ مشاركة. تُفحَص بيدٍ على iPhone من Safari، ومن رابط
> فيسبوك، وعلى Android — **والنتيجة تُكتب في سطر الإغلاق، ولا تُدَّعى.**
> **V.07.5 = `420`** — الدليل يشتغل بلا إنترنت. **ويقفل مجموعته وهي هو.**
> ⚠️ **والشبكة الكاملة خضراء على البناءين: 102 تشغيلة · 51 سويتاً ·
> 5,754 بنداً · صفر أحمر · صفر انهيار.** ولا بند سقط أوّلاً — ولا سويت
> أُعيدت كتابتها، لأنّ الدفعة تضيف ولا تنقض قراراً.
> ⚠️ **و`395` لم يرفع الرقم**: أداةُ تشغيلٍ ولا سطر في `js/` ولا `styles/`.
> ⚠️ **وبناءان ساقطان على الفرع — `9399c28` و`6bb32a1`** — سببهما مفتاحُ
> شرحٍ `"//"` كتبتُه داخل `vercel.json`، ومخطَّطُ Vercel يرفض خاصّيّةً
> زائدة. صُحِّح في `6459f42` والنشرةُ عادت **READY**، **ولم يبلغ الإنتاجَ
> شيء**: `main` واقفةٌ على `f9d2e4d` منشورةً طوالَ الوقت — قاعدةُ النشر
> تعمل، لا حظّاً.
> **V.07.4 = `337`** — صفّان مموّلان، والباقي بالمسافة. **ويقفل مجموعته وهي هو.**
> ⚠️ **والشبكة الكاملة خضراء على البناءين: 100 تشغيلة · 50 سويتاً ·
> 5,712 بنداً · صفر أحمر · صفر انهيار.** وبها تحرّك `main` إلى V.07.4.
> ⚠️ **وثمانية عشر بنداً سقطت أوّلاً في ثلاث سويتات — `v15` و`v21` و`v49` —
> ولا واحد منها عطبٌ في الشيفرة:** كلّها كُتبت لنموذج `330` (كلّ مشترك
> يُرفَع، والطبقة مرتَّبةٌ بالمسافة)، و`337` غيّر النموذج بقرار رامي. وأُعيدت
> كتابتها بتعليقٍ يسمّي الانقلاب، ولا واحد لُيِّن.
> ⚠️ **وأدقّها `v49 · 2.1`**: `330` وعد بترتيب الطبقة بالمسافة، و`337` يسحب
> صفّين بالتناوب فيكسر ذلك **عمداً** — فتجميدهما كان سيكون هو العطب.
> والمقيس صار ما يملكه القارئ: **كلّ ما تحت الشريط أميالٌ صاعدة.**
> ⚠️ **وقياسٌ يجعل القرار رخيصاً اليوم**: مشتركٌ واحد في كلٍّ من التصنيفات
> الأربعة، فـ«كلّ المشتركين فوق» و«صفّان» شيءٌ واحد حرفيّاً الآن — والفرق
> يظهر يوم تمتلئ المطاعم (138 نشاطاً).
> ⚠️ **و`subFeatures` مفتاحٌ ميّت لا يقرؤه أحد** — الوعد كُتب في `planRank`،
> وهو ما تطبعه شاشة الاشتراك فعلاً.
> ⚠️ **ومجموعة `330` + `335` أُغلقت خضراء** — الشبكة الكاملة على النسختين
> بعد `336` وتصحيحه: **98 تشغيلة · 49 سويتاً · 5,664 بنداً · صفر أحمر ·
> صفر انهيار.** وبها تحرّك `main` من V.07.0 إلى V.07.3.
> ⚠️ **وسويتان انهارتا في التشغيلة التي قبلها، وكلتاهما عطبُ حارسٍ لا عطب
> برنامج:** `v50` كان يحسب «اليوم» بساعة الحاوية والتطبيق يحسبه بساعة
> المتصفّح — **ويومٌ كامل بينهما** (الحاوية 30 والمتصفّح 29) — و`v51` كُتبت
> على النموذج الذي صُحِّح. الأولى **توأمٌ زمنيّ للانتظار برقمٍ ثابت**:
> اختبارٌ يصحّ بين ساعتين بعينهما ليس اختباراً.
> **V.07.3 = تصحيح `345 · 5`** — الرأسان يُطويان، وبلا سهم.
> ⚠️ **والخطأ في مواصفة `345` لا في تنفيذها**: كُتب فيها «يصير عنوان قسمٍ
> مفتوحاً دائماً» ورامي قال «شيل السهم، ولمّا حدا يضغط تفتح معه» — فسقطت
> قاعدة «مطويّاً لا يمرَّر الدرج» (الزائر 1049/844)، **وعادت 844/844.**
> ⚠️ **وسبعة من البنود الثمانية عادت خضراء بذاتها، ومطابقةً حرفيّاً لما
> كانت** — أوّل جوابي عليها كان تليين خمس سويتات، وهو محو شاهد. `v7` وحده
> انقلب بتعليقٍ يسمّي الانقلاب.
> **V.07.2 = `336`** — الفرز الذي اختاره القارئ يحكم الطبقتين.
> ⚠️ **و`336` هو قافل مجموعة `330` + `335`** — الشبكة الكاملة معه على
> البناءين، **وبعد خُضرتها وحدها يُدمَج كلّ ما على الفرع إلى `main`.**
> ⚠️ **وقرار رامي فيه:** مع «مفتوح الآن» الانفتاح مفتاحٌ أوّل والاشتراك
> فاصلٌ داخله — لأنّ بندَي `336` (2 و3) لا يصحّان معاً: مشتركٌ واحد مفتوح
> وثلاثة مغلقون **و87 مجّانيّاً مفتوحاً وقت القياس**. ⚠️ **والعدد ليس ثابتاً:**
> مقيسٌ على شجرة `cdd6b46` عبر اليوم — **272 · 370 · 331 · 258 · 119** عند
> 10 و14 و18 و20 و22، و**87 آخر المساء**. **وكان مكتوباً هنا 176، وهو قياس
> ساعةٍ أخرى** — والتصحيح ليس «176 ← 87» بل أن يُكتب المدى لا رقمٌ عارٍ،
> **لأنّ رقماً يتغيّر كلّ ساعة يصير خطأً لحظةَ يُكتب كأنّه حقيقة.** والحجّة
> لا تتغيّر: ثلاثة مغلقين فوق 87 هي الحجّة نفسها فوق 370.
> **والوعد الذي تغيّر مكتوبٌ صراحةً:**
> «المشترك لا ينزل تحت مجّانيّ» صار «لا ينزل تحت مجّانيٍّ في حالته نفسها».
> **V.07.1 = `345`** — خمس ملاحظاتٍ من الشاشة.
> ⚠️ **و`345` على فرع العمل وحده، بلا دمجٍ إلى `main`** — الشبكة حمراء عند
> `v8`، والقاعدة تمنع تحريك الإنتاج قبل خُضرتها. **و`main` باقٍ عند V.07.0.**
> ⚠️ **والترتيب من هنا:** `345` (فرع) ← **`336`** (يقفل `330`+`335`) ←
> الشبكة كاملةً ← **وعندها وحدها يُدمَج الاثنان إلى `main`.**
> **V.07.0 = `335`** — التهاني: أداةٌ واحدة لكلّ مناسبة.
> ⚠️ **و`335` هو قافل المجموعة** (`330` ثمّ `335`) — الشبكة الكاملة شُغِّلت
> معه على النسختين.
> ⚠️ **و`080` أُلغي بنصّ `335`** — قسمه الرابع نزل هنا، وأرضيّته تغيّرت
> بنزول `ramadanDates` وشعار العرض بنسختيه. **وربط تواريخ رمضان بالموسم
> يخرج إلى `340` بقرار رامي.**
> وسويتات `330` وحدها على النسختين: **`3 · 15 · 18 · 21 · 33 · 34 · 37 · 40 · 49`
> خضراء — 452 بنداً**. ⚠️ **ثلاثٌ منها احمرّت أوّلاً وكُتبت من جديد**، ولا واحدة
> منها عطبٌ في التطبيق: كلّها بنودٌ مكتوبةٌ على نموذج «صفٌّ واحدٌ مرفوع».
> ⚠️ **وبندان منها كانا يمرّان وهما لا يقيسان شيئاً** (`v21 · 4.7` وثلاثةٌ من
> `v34`) — يمرّان على قائمةٍ فارغة، وهذا أسوأ من الأحمر لأنّه مُصدَّق.
> ⚠️ و`325` مع `326` **يلغيان `320`** — تغيّرت أرضيّته بنزول قراراتٍ فيه.
> ⚠️ و`315` **وصل باسم `300` وأُعيد ترقيمه** — `300` رقمٌ ملغى حلّ محلّه `305`.
> ⚠️ ومجموعة «حسابي»: **`315` + `325` + `326`** — الشبكة الكاملة في آخرها.
> ⚠️ و**الشبكة الكاملة تُشغَّل مرّةً في آخر المجموعة لا مع كلّ دفعة** — قرار رامي 28 أغسطس.
> ⚠️ **و`main` هو الإنتاج** — قرار رامي 29 أغسطس، ومقيس: مشروع Vercel مربوطٌ
> بـ`main`، وكلّ دفعٍ عليه يُنشَر إنتاجاً مباشرة (آخر أربع عشرة نشرة كلّها
> `target: production`). **فالدفع إلى فرع العمل فوريّ كما هو — لا يضيع شغل،
> وللفرع رابط معاينته — ولا يُدفَع إلى `main` ولا يُدمَج فيه إلّا بعد أن
> تخضرّ الشبكة الكاملة على البناءين.** يعني **الإنتاج يتحرّك عند حدود
> المجموعات وحدها**، وهذا مقصود. ⚠️ **وإن احمرّ بندٌ بعد أن صار الإنتاج
> عليه: يُبلَّغ فوراً وباسم البند، ولا يُصلَح بصمت — والقرار في التراجع
> لرامي وحده.**
> مجموعة الأمان: **`307` + `308`**.
> ⚠️ و`307` **وصل باسم `320` وأُعيد ترقيمه**، فترتيب الاسم هو ترتيب الإرسال:
> `307` ← `310` ← `315` ← `320`.
> و`300` **أُلغي** — حلّ محلّه `305`.
> و`290` **أُلغي** — حلّ محلّه `295`.

> **يُقرأ هذا الملفّ في أوّل كلّ جلسة، قبل أيّ تعديل.**
> جلسةٌ جديدة لا تعرف الترقيم، ولا ما هو مؤجَّلٌ بقرار، ولا ما رُفض من
> قبل — **فتبلّغ عن المؤجَّل كأنّه اكتُشف، وترقّم ملفّاً برقمٍ مأخوذ،
> وتقترح ما نوقش وأُغلق.**
>
> **ومعه ملفّان في أوّل كلّ جلسة:**
> **`docs/سجل-القرارات.md`** — ما نوقش وأُغلق، **وما رُفض وسببه**.
> **و`docs/تقارير/`** — كلّ تقريرٍ أوّل سطرٍ فيه `الحالة: جديد`.
>
> ⚠️ **والتقرير يُقرأ ولا يُنفَّذ**: لا سطر شيفرةٍ يتغيّر إلّا بملفٍّ
> مرقَّم. ⚠️ **والسجلّ يُقرأ ولا يُكتَب فيه** — الحالة هنا، لا هناك.

---

## 1) قواعد ثابتة لا تُناقَش

- **الأشهر بالأسماء الأجنبيّة مكتوبةً بالعربيّة**: يناير · فبراير …
  ديسمبر. ولا «كانون» ولا «تمّوز» ولا «آذار» ولا «تشرين».
- **الأرقام بالخانات الإنجليزيّة (0-9) دائماً**، ولا رقم عربيّ-هنديّ.
  (والاستثناء الوحيد: ما يكتبه المستخدم بيده لا يُحوَّل.)
- **أسماء الأماكن والمدن والشوارع لا تُترجَم ولا تُعرَّب** — «Houston»
  داخل الجملة العربيّة. ووسوم البحث العربيّة تبقى عربيّة: هي غير مرئيّة،
  وهي ما يجعل «هيوستن» تجد 378 نشاطاً أسماؤها إنجليزيّة.
- **كلّ قيمة لم تُكتَب في `i18n.js` تمرّ بـ`esc()` قبل `innerHTML`. بلا
  استثناء** — والنسخة الملفّ-الواحد بلا طبقة CSP ثانية، فما نجا يُنفَّذ.
- **كلّ دالّة تُرجع `HTML` ينتهي اسمها بـ`Html`**، وما لا ينتهي اسمه بذلك
  يُرجع نصّاً ويجوز أن يمرّ على `esc()`.
- **نصوص المحتوى ليست `HTML`.**
- **ولا شرط `if` باسم مدينة**: التغطية تُقرأ من `CITY_POINTS`.
- **الأذان يُحسَب والإقامة تُنشَر**، ولا يُخلطان. ولا نُسنِد مذهباً ولا
  طائفةً لمسجد أو كنيسة أبداً.
- **ولا مساحة إعلانيّة على `#/prayer` ولا `#/mass` ولا صفحة مسجد أو
  كنيسة.**
- **الشبكة الكاملة تُشغَّل مرّةً واحدة في آخر المجموعة، لا مع كلّ دفعة**
  (قرار رامي 28 أغسطس). والدفعة تشغّل السويتات التي تمسّها وحدها.
  ⚠️ **والملفّ الذي يقفل المجموعة يقول ذلك في رأسه** (قرار رامي 29 أغسطس)
  — **في الرأس لا في الذيل، ليُقرأ قبل العمل لا بعده.** وجلسةٌ لا تعرف
  أنّها تحمل آخر دفعةٍ في المجموعة تشغّل الملامِسة وتُبلّغ بالخُضرة،
  **فتُغلَق المجموعة ولم تُختبَر كاملةً قطّ** — وهو بعينه العطل الذي وُجدت
  قاعدة المجموعة لتمنعه.
  **ولا تُغلَق مجموعة ولا يُقال إنّها انتهت قبل أن تخضرّ الشبكة كاملةً على
  النسختين.** وإن لم يقل الملفّ إنّه القافل، **يُسأل ولا يُفترَض**.
- **رقم `APP_VERSION` في `js/data.js` يُرفَع داخل الدفعة**، ولا تُغلق دفعة
  ورقمُها مخالفٌ للرقم في رأس `CLAUDE.md`. (انحرف مرّتين.)

---

## 1.ب) بوّابةُ الإطلاق — تُقرأ يومَ يُربَط النطاق، لا تُنفَّذ اليوم

⚠️ **مكتوبةٌ لتُقرأ، وخطوةٌ تُنفَّذ قبل أوانها أسوأ من واحدةٍ تُنتظَر.**

```
قبل نشر اسم النطاق:
  1  حذف DEMO_BUSINESSES وDEMO_REVIEWS من js/data.js
  2  فتح robots.txt
  3  مراجعة start_url وscope في manifest.json على النطاق الجديد
  4  connect-src في سياسة الحماية تعرف النطاق الجديد
  5  تحويل من العنوان المؤقّت — ومن ثبّت البرنامج عليه يبقى عليه بغيره
```

⚠️ **و`robots.txt` مؤقّتٌ بقرار**: `Disallow: /` للكلّ، **ويُفتَح يوم
يُربَط `arabna.app` لا قبله.** وفتحُه قبل النطاق يجعل العنوانَ المؤقّت هو
ما تعرفه Google، **ونقلُ نتيجةٍ مفهرسةٍ من عنوانٍ إلى عنوانٍ أصعبُ من
فهرستها أوّلَ مرّةٍ على الصحيح.** ولا `sitemap.xml`: لا يُبنى دليلٌ لمن
هو ممنوعٌ من الدخول.

⚠️ **والخطوة 1 ليست عاجلة بعد `510`**: البيانات المخترَعة صارت خلف مفتاحٍ
**مطفأٍ افتراضيّاً**، ولا يستطيع زائرٌ تشغيلَه — التشغيلُ لا يعمل إلّا على
جهاز من شغّله. **وبقاؤها في مستودعٍ علنيٍّ بقاءُ نصٍّ مقروء، لا بقاءُ
تقييمٍ معروض. والفرق كبير ولا يُخلَط.**

---

## 2) فجوات معروفة ومؤجَّلة — لا يُبلَّغ عنها كأنّها جديدة

| الفجوة | القرار | متى تُفتَح |
|---|---|---|
| **514 من 514 نشاطاً بلا إحداثيّات** — ولا واحد له `lat`/`lng` | مؤجَّلة | حين تصل الإحداثيّات — عملُ بياناتٍ خارج التطبيق · ⚠️ **كان مكتوباً هنا «480 والـ34 الباقية بذور»، وهو خطأ**: الـ34 مراكزُ أكواد بريديّة ونقاطُ مدنٍ في `js/data.js`، لا أنشطة. مقيس بتشغيل المستودع |
| ردّ المبالغ بلا زرّ | مؤجَّلة | دفعة السيرفر |
| لا بوّابة دفع حقيقيّة — الدفع محاكى | مقصود | دفعة السيرفر |
| النسخة الملفّ-الواحد بلا طبقة CSP ثانية | مقبول بقرار | لا يُفتَح — `'unsafe-inline'` شرط بنائها |
| لا قسم مستخدمين في اللوحة، ولا عدّ يعبر الأجهزة | مقصود | دفعة السيرفر — حسابٌ واحد على جهاز واحد اليوم |
| الإشعارات الفوريّة غير موصولة بخدمة | مؤجَّلة | دفعة السيرفر |
| «تنبيه قبل الأذان» مفتاحٌ بلا صوت | مقصود، ومكتوبٌ على الشاشة | دفعة السيرفر |
| القائمة الجانبيّة تتجاوز الشاشة بأكثر من صفّين حين تُفتح مجموعة | **قرار رامي** | حين يقرّر أيّ صفّ يُحذَف |
| جمعة وإقامة 33 مسجداً فارغة | مؤجَّلة | تصل من المساجد ومن تصحيحات الجالية |
| رمضان: ثلاث خصائص على أربعة أنشطة، كلّها بذور | عمل بيانات | شهرٌ قبل رمضان |
| دليل المستجدّ: ثمانية أقسام بنصٍّ مؤقّت يقول ذلك | **النصّ مكتوب** — 17 قسماً بلغتين | ينتظر إرسال الملفّ `030` |
| الصفحات القانونيّة مسوّدات | ينتظر محامياً | قبل الإطلاق |
| 29 بذرة تطويريّة + التقييمات المزروعة | تُحذف قبل الإطلاق | مصفوفتان ومفتاح في اللوحة |
| `setUserRegion` تكتب `manual: true`، فمن اختار منطقةً مرّة يدخل مسار السؤال لا التحديث الصامت | **ينتظر قرار رامي** — سطرٌ واحد في ملفٍّ منفصل |
| `SUPPORT_PHONE` فارغ | ينتظر رامي | رقمٌ حقيقيّ |
| كلمة مرور اللوحة يضعها رامي بيده | **بقرار أمنيّ** | لا تُرسَل ولا تُكتَب في شيفرة · ⚠️ **والكلمة القديمة التي كانت في `store.js` مرفوضةٌ بقاعدة `PW_BRAND`** — كانت تنزل مع الملفّ إلى كلّ متصفّح وما زالت في تاريخ المستودع، فلا تُستعمل |
| 16 مشروعاً في فريق Vercel والقاعدة الأولى تقول واحد | ينتظر رامي | تنظيف الفريق |
| مفاتيح اللوحة تغيّر جهاز الإدارة وحده | مقصود | دفعة السيرفر |
| **لا سويت يحرس صفّ الروابط نفسه** — ستّة صفوف، ورابط كلٍّ منها، وسطر «قريباً» يتبع السجلّ | مؤجَّل **بقرار `295`** | ملفُّ سويتٍ خاصّ به في دفعةٍ لاحقة · و`v14` يحرس اليوم وصول البريد وحده |
| **شاشة «تسجيل الدخول» لا تتحقّق من كلمة المرور** — أيّ كلمة، حتّى الفارغة، تُدخِل؛ و`checkUserPassword()` مكتوبةٌ ولا يستدعيها أحد | **تنتظر كلمة رامي** — نصّ `315` يمنع كتابة سطرٍ قبلها | إمّا تقول الشاشة الحقيقة، وإمّا تُقفَل حتّى السيرفر · وباقي `315` نزل بدونها |
| ⚠️ **وعد «المشترك لا ينزل تحت مجّانيّ» صار «في حالته نفسها»** — مع «مفتوح الآن» وحده | **قرار رامي في `336`** | مكتوبٌ صراحةً لا مخفيّ · والعرض الافتراضيّ لم يتغيّر بحرف |
| **سلسلة ترتيب الدليل** — أُصلحت في `330` بنموذج الطبقتين: من دفع فوق، والأقرب أوّلاً | **نزل قرار رامي ونُفِّذ** | ⚠️ **وسقفُ عدد الصفوف الموسومة يبقى قرارك** — نظريٌّ اليوم: أربعة مشتركين في الدليل كلّه |
| `v15` فيه انتظارٌ برقمٍ ثابت يسقط تحت ضغط التشغيل الكامل — ثمانية بنود من خطوةٍ واحدة | مؤجَّل | ملفُّ سويتٍ خاصّ به · والنمط جاهز في `235` |
| الاشتراك مفردٌ بينما الملكيّة صارت جمعاً — مالك ثلاثة فروع يشترك مرّةً واحدة | **مقصود** | دفعة السيرفر — الاشتراك يمسّ الدفع، والدفع ليس مكانه المتصفّح |
| **زرّ «ثبّته بضغطة» مستحيلٌ على iOS** — لا واجهةَ تثبيتٍ برمجيّة، وApple لا تنشر واحدة | **قيدٌ خارجيّ، لا فجوة** | لا يُفتَح · وما يقوم مقامه: ثلاث خطواتٍ مرقّمة، وسطر «مرِّر للأسفل»، وحلُّ متصفّح فيسبوك أوّلاً |
| **روابط المتاجر فارغة** — `PLAY_URL` و`APPSTORE_URL` في `js/store.js` | ينتظر رامي | سطرٌ واحدٌ يوم ينزل المتجر، والبرنامج يتحوّل من نفسه · ⚠️ **ولا رابطَ يُكتب قبل أن يوجد** |
| **الدعوةُ في «الإعدادات» لا في «حسابي»** — والملفّ يقول «حسابي» | **أقرّه رامي في `425`، وحجّتُه هي المسجَّلة** | `#/settings` بلا `requireTier` أصلاً، وفي البرنامج نصٌّ منشورٌ يقول للزائر إنّ اللغة والمظهر والحجم تعمل بلا تسجيل · ⚠️ **ولا تُوضع في الاثنين**: شيءٌ واحدٌ في موضعين يفترقان بعد تعديلين |
| ⚠️ **من ثبّت البرنامج قبل `485` وعَلِقت أيقونتُه لا يصله التحديث** — لأنّ الصفحة لا تُفتَح أصلاً | **يُقال لهم بيد رامي** | **1)** تُحذَف الأيقونة من الشاشة · **2)** إعدادات الهاتف ← Safari ← Advanced ← Website Data ← يُمسَح المضيف · **3)** يُفتَح الرابط من Safari · **4)** ثمّ تُعاد الإضافة. ⚠️ **ولا يُقال «حدّثوا التطبيق»**: لا تحديثَ من داخل شيءٍ لا يفتح |
| ⚠️ **`start_url` لا يُلمَس أبداً** — لا `id` في المانيفست، فالهويّة تُشتقّ منه | **قرارٌ قائم** | تغييرُه يجعل الهاتف يراه تطبيقاً آخر، ومن ثبّته يبقى على أيقونةٍ ميّتةٍ بلا تحديث · يحرسه `test_v56 · 4.1` و`4.2` |
| ⚠️ **Safari تحذف تخزين التبويب بعد سبعة أيّامٍ بلا تفاعل** — `localStorage` وIndexedDB ومخزنَ عامل الخدمة الذي بناه `420` | **قيدُ منصّةٍ، لا فجوة** | ⚠️ **والمضافُ إلى الشاشة مستثنى** — فـ`425` شرطُ بقاءٍ لا تحسين · والبوّابة الواحدة تجعل تبديلَ الخلف سطراً واحداً يوم يأتي الغلاف |
| ⚠️ **قاعدتان خرجتا من حمراوَي `430`** — كلَّ نصٍّ يُغيَّر يُفتَّش عنه في `tools/e2e/` **قبل** الشبكة · والفحص يقرأ الشيفرة لا النثر عنها | **مكتوبتان في `CLAUDE.md`** | والثانية للمرّة الثانية بعد `420` — ولذلك كُتبت بالحادثتين لا بواحدة |
| **الغلاف الأصليّ نفسه** — Mac وXcode 26 فأعلى وحساب Apple (99 دولاراً سنويّاً فرداً أو مؤسّسة) | **خارج ما يُكتب في ملفّ** | ينتظر رامي · والبابُ مبنيٌّ في `430` فلا يُكتب مرّتين |
| **جدولةُ تنبيه الأذان** — الحسابُ مبنيٌّ ومقيس (`alertSchedule`، 64 فأقلّ)، والجدولةُ نفسها لا | مؤجَّلة **بقيدٍ منصّة** | الغلاف الأصليّ · ⚠️ **ولا الويب**: `Notification Triggers` أُوقِف تطويرها، فلا سبيل لجدولة تنبيهٍ لوقتٍ قادم |
| ⚠️ **لا إشعارات نظامٍ في البرنامج البتّة** — `new Notification` و`requestPermission` و`showNotification` و`PushManager`: **صفرٌ في `js/` و`sw.js` كلِّها**. فما نسمّيه «إشعارات» لائحةٌ داخل البرنامج (`pushNotif` تكتب في `state.extraNotifs`)، ومفتاحُ تنبيه الأذان يرفع علامةً تُولّد بنداً فيها — **لا تنبيهاً يصل هاتفاً مُقفَلاً** | **قياس رامي، وقد نقل الشرط** | **بعد السيرفر** — تحتاج خدمة دفعٍ وخادماً · ⚠️ **والشرط ليس «يوم تنزل التنبيهات» بل «يوم تصير صحيحة»** |
| **«تنبيه الأذان» لا يُذكَر سبباً للإضافة، ولا يُضاف لاحقاً** | **قرار رامي في `425`** | حتّى يصير صحيحاً · ويحرسه `test_v54 · 8.6` و`8.7`: صفرُ نداءٍ لإشعارات النظام، مؤكَّداً بجانب «لا نصَّ دعوةٍ يذكر تنبيهاً» |
| **الفحص اليدويّ الثلاثيّ لـ`425`** — iPhone من Safari · iPhone من رابط فيسبوك · Android | **لم يُجرَ ولا واحدٌ منه** | ⚠️ **لا يُجرى من الحاوية**: Chromium بلا `display-mode` ولا لوحة مشاركة ولا `beforeinstallprompt` حقيقيّ. يحتاج هاتفاً على معاينة Vercel · وما في الشبكة **قراءةٌ بانتحال UA لا فحص** |
| **قائمة المسارات في `test_v37` مكتوبةٌ بيد** — وتعليقُها يقول إنّ ذلك مقصود: قائمةٌ منسوخةٌ من `app.js` مرآةٌ لا فحص | **قرارٌ قائم** | لا يُفتَح — وقد التقطت `#/install` في `425` كما وُضعت له |
| **الشبكة لا تغطّي عامل الخدمة** — لا تسجيل خارج https، وهو ما يحمي الشبكة نفسها من ملفّاتٍ بائتة بين السويتات | **مقصود، ومكتوبٌ لا مُدَّعى** | يُفحَص بيدٍ على معاينة Vercel والشبكة مطفأة · واختبارٌ لا يُشغَّل يُقال إنّه لا يُشغَّل |
| **أوّل زيارة ما زالت 1,895 كيلوبايت في 25 طلباً** — عامل الخدمة يخفّف الزيارة الثانية لا الأولى | مؤجَّلة | دفعة الأداء — ومعها صورة الرأس |
| **`vercel.json` لا يُشرَح فيه شيء** — لا تعليقات في JSON، ومفتاح `"//"` مرفوضٌ بمخطَّط Vercel قبل أن يبدأ البناء | **قاعدة، لا فجوة** | السببُ في `CLAUDE.md` وفي رسالة الالتزام · ⚠️ **ولا سويت يلتقط هذا**: `test_v53 · 5.2` يقرأ الملفّ كـJSON ومفتاحٌ زائدٌ يُقرأ سليماً — الرافضُ مخطَّطُ Vercel، وموضعُ جوابه `errorMessage` في النشر |
| صورة الرأس 333 كيلوبايت لصندوق 66×65 (كانت 831) | مؤجَّلة | دفعة الأداء — تُولَّد العلامة بنحو 200px بدل 659 |
| **معاينة الرابط تُظهر البرنامج لا الصفحة** — ما بعد `#` لا يصل الخادم | مؤجَّلة **بقيدٍ معماريّ** | دفعة السيرفر — مساراتٌ حقيقيّة، وهي نفسها حاجة Universal Links |
| **صورة المشاركة تقول Houston** — تُقرأ على خادم واتساب قبل أن يفتح أحد | مقصود | تُبدَّل يدويّاً عند التوسّع |
| مجموعة «المساعدة» تتجاوز — **زائر 131 · عضو 56** بعد `305` | **قرار رامي** | نزلت عمّا كانت مع البلاطات (131 و56 مقابل 95 و12 قبلها) |
| الدرج **مطويّاً 0 تجاوز** للدورين وفي المظهرين — ومع مجموعة مفتوحة **زائر 181 · عضو 106** بعد `305` (كانت 195 و112 قبل البلاطات، و231 و156 معها) | **قرار رامي** | ⚠️ **و«صفٌّ واحد يسدّها» قِيست وثبت أنّها كاذبة** — الورقة 50 بكسلاً فالعدد أربعة لا واحد · و`.drawer-panel` تنسحب إلى آخرها فلا صفّ محجوب |

---

## 3) الترقيم

**آخر رقم مستعمل في السويتات: `test_v44.mjs`.**

| النطاق | ما فيه |
|---|---|
| v3 – v35 | الدفعات من الأولى إلى V.04.2 |
| **v36** | تقويم المناسبات (V.04.3) |
| **v37** | المسح العامّ: كلّ شاشة وما لا يجوز أن يكون فيها |
| **v38** | الأدوار الأربعة |
| **v39** | «تلقائي» والقارئ خارج المنطقة (V.04.5) |
| **v40** | دفعة 9ج — الهويّة البصريّة ووسوم المشاركة (V.04.7) |
| **v41** | تفضيلات الجهاز: الإعدادات للجميع وزرّ المظهر (V.04.8) |
| **v42** | الخروج يُنهي الملكيّة على الجهاز (V.05.2) |
| **v43** | الملكيّة جمعٌ لا مفرد، والترحيل من المفتاح القديم (V.05.4) |
| **v44** | صور العرض: العلامات الاثنتا عشرة والإيموجي والصورة المرفوعة (V.05.9) |

⚠️ **وقد وقع الاصطدام فعلاً في هذه الدفعة**: ملفّ الفحص اليوميّ طلب
`test_v36` و`test_v37`، و**`v36` كان مأخوذاً منذ V.04.3** — فصارا `v37`
و`v38`. **وهذا بالضبط ما يمنعه هذا القسم.**

**الملفّ الجديد يأخذ الرقم التالي، ولا يُعاد ترقيم ما سبقه.**

⚠️ **ورقم السويت يُحجَز من المستودع لحظة الإرسال، لا من ورقةٍ عند أحد.**
فبين كتابة ملفّ الفحص اليوميّ وإرساله نزلت `V.04.3` وأخذت `v36`،
**والرقم الذي حُجز في الرأس كان قد صار مأخوذاً على القرص.**

---

## 4) ما ينتظر الإرسال

⚠️ **هذه القائمة يكتبها رامي، لا الجلسة.** والجلسة ترى ما وصلها فقط،
فلا تكتب هنا «لا شيء» لأنّ صندوقها فارغ — **الصندوق الفارغ ليس مشروعاً
منتهياً.** ولا يُحذف سطرٌ من هنا إلّا بعد أن ينزل ما فيه فعلاً.

| الرقم | الملفّ | يعتمد على |
|---|---|---|
| 030 | دليل الواصل — النصّ الكامل (17 قسماً) + أداة الفيضان | — |
| 040 | دفعة 10أ — المجلّة | — |
| 050 | دفعة 10ب — الأقسام الفارغة | — |
| 060 | دفعة 10ج — المراجعة قبل الدفع | — |
| 070 | دفعة 10د — شريط الصلاة | — |
| ~~080~~ | **ملغى بنصّ `335`** — التهاني نزلت فيه، ورمضان خرج إلى `340` | — |
| 090 | دفعة 10ز — زرّ التبليغ | — |
| 100 | دفعة 10ح — العروض | — |
| 110 | دفعة 9د — الفلاتر والترتيب والأسماء | — |
| 120 | دفعة 9هـ — صورة المشترك في الدليل | — |
| 130 | دفعة 9و — شاشة الحاسوب | — |
| 140 | ضوابط الصور | — |
| 150 | الأسماء اللاتينيّة (JSON) | — |
| 160 | الإحداثيّات — عمل بيانات | — |
| 165 | حذف الدوالّ الميتة | **يُرسَل أخيراً** — بعد أن تنزل الدفعات كلّها |
| 340 | ربط تواريخ رمضان بالموسم | خرج من `335` بقرارك |

**وقد نزل من القائمة:** `005` و`006` · `010` تقويم المناسبات (V.04.3) ·
`170` و`175` (V.04.4) · `015` و`025` (V.04.5) · `180` و`185` (أدوات
ووثائق) · `190` (V.04.6) · `020` (V.04.7) · `195` (V.04.8) ·
`200` (V.04.9) · `210` (وثائق) · `220` (V.05.0) · `205` (V.05.1) · `225` (V.05.2) · `230` (V.05.3) · `235` (أداة) · `240` (V.05.4) · `245` و`270` (سويتات) · `255` (V.05.5) · `260` (V.05.6) · `265` (V.05.7) · `280` (V.05.8) · `275` (V.05.9) · `285` (V.06.0) · `295` (V.06.1) · `305` (V.06.2) · `310` (V.06.3) · `307` (V.06.4) · `308` (V.06.5) · `315` (V.06.6) · `325` (V.06.7) · `326` (V.06.8) · `330` (V.06.9) · `335` (V.07.0) · `345` (V.07.1) · `336` (V.07.2) · `385` (الطابور) · `337` (V.07.4) · `395` (أداة تشغيل) · `420` (V.07.5) · `425` (V.07.6) · `430` (V.07.7) · `480` (V.07.8) · `485` (V.07.9) · `490` (V.08.0) · `510` (V.08.1).
**و`080` أُلغي بنصّ `335`** — قسمه الرابع نزل فيه، وباقيه تغيّرت أرضيّته.
**و`320` أُلغي بنصّ `325` و`326`** — تغيّرت أرضيّته بنزول قراراتٍ كانت فيه.
**و`300` أُلغي بنصّ `305` نفسه** — كان ناقصاً بندين: الصفّ المكرّر، والسقف
الذي عاد في `305` **أخفض ممّا بدأ**؛ فلا يُلصَق.
**و`290` أُلغي بنصّ `295` نفسه** — كُتب قبل نزول `275` و`280`، وكان يطلب
ما صار في المستودع فعلاً؛ فلا يُلصَق.
