الحالة: قُرئ — البنود في الملفّ 435

# الفحص اليوميّ — 2026-08-30

**الكوميت المفحوص:** `59b1c81` — «إغلاق 337: الشبكة الكاملة خضراء».
**النسخة:** V.07.4. **النسختان:** `index.html` و`index-single-file.html`.
**المدّة:** المجموعة الكاملة 11:04 ← 11:41 UTC — **37 دقيقة**؛ وفحصي بيدي
وإثبات أسنانه بعدها حتّى 12:04.
**شجرة العمل بعد الفحص:** `git status --porcelain` **فارغ** — لا شيفرة
عُدِّلت، ولا commit، ولا فرع. وكلّ ما لُمس أثناء إثبات الأسنان أُرجع
بـ`git checkout`، وأُعيد بناء `index-single-file.html` إلى ما كان.

---

## 1) النتيجة في سطر واحد

الشبكة الكاملة والفحص الساكن خضراوان بلا حرف أحمر — **وفحصي بيدي وجد عطلاً
واحداً**: زرّ «إنشاء الحساب» على `#/settings` عند الزائر **مقصوصٌ 16 بكسلاً**
على النسختين وباللغتين وعلى المقاسات الأربعة، **والسويت الذي وُضع ليمسكه
(`v41 · 2.5`) يقيس المحور الغلط فيمرّ أخضر فوقه.**

---

## 2) أرقام ما شُغِّل

### الفحص الساكن — `tools/audit/wiring.mjs`

```
7 بنود · 0 أحمر
```

### المجموعة الكاملة — `tools/e2e/run.sh` على النسختين

```
100 تشغيلة · 50 سويتاً · 5,712 تأكيداً · 0 أحمر · 0 انهيار
```

**والمقارنة بالرقم المسجَّل في `docs/الحالة.md`:**

| | المسجَّل | المقيس اليوم |
|---|---|---|
| التشغيلات | 100 | **100** |
| السويتات | 50 | **50** |
| التأكيدات | 5,712 | **5,712** |
| الأحمر · الانهيار | 0 · 0 | **0 · 0** |

مطابقٌ إلى البند الواحد. **ويومٌ بلا دفعة يجب ألّا يغيّر الرقم، ولم
يغيّره.**

---

## 3) ما غطّاه فحصي أنا — بالمدى المقيس لا بالوصف

⚠️ **ولا قائمة مكتوبة بيدي فيه:** المسارات تُستخرَج من `js/app.js` وقت
التشغيل (**55 مساراً**)، والنصوص المتوقَّعة تُقرأ من `js/i18n.js` وقت
التشغيل (**1,752 مفتاحاً** بعد استبعاد ما يظهر اسمه داخل قيمةٍ ما، والاستبعاد
مشتقٌّ لا مكتوب).

| ما فُحص | المدى المقيس |
|---|---|
| الشاشات الـ41 | **164 زيارة** — 41 × لغتين × نسختين |
| الروابط الداخليّة | **1,316 رابطاً** قُوبل كلٌّ منها بـ55 مساراً مستخرجاً من `app.js` |
| مفاتيح i18n | **1,752 مفتاحاً** × نصّ كلّ زيارة |
| الأدوار | **30 زيارة** — 3 أدوار × 5 مسارات × نسختين، ودور الإدارة معها |
| لوحة الإدارة | **16 فتح تبويب** — 8 تبويبات × نسختين، وكلّ واحدٍ يُقاس نصّه |
| العروض | **192 قياساً** — 12 شاشة × 4 مقاسات × لغتين × نسختين |
| خارج التغطية | **8 زيارات** — شاشتان × لغتين × نسختين |
| **الحصيلة** | **79 بنداً · 63 أخضر · 16 أحمر** — والأحمر كلّه العطل نفسه |

⚠️ **وقياس العرض لا يقرأ `documentElement.scrollWidth`**: `.app-main` فيها
`overflow-x: hidden`، فالزائد **يُقصّ ولا يُمرَّر**. فيُقاس صندوق كلّ عنصرٍ
داخل الشاشة مقابل إطارها، ويُستثنى ما جدُّه ممرِّرٌ أفقيٌّ بقرار (شريط
الصور، «مميّز هذا الأسبوع»، السلايدر).

---

## 4) البند الذي خرج — مقيساً بالأرقام

### زرّ «إنشاء الحساب» على `#/settings` عند الزائر مقصوصٌ 16 بكسلاً

`js/screens/profile.js:994-995`

```html
<button class="btn btn-gold btn-block" data-route="#/auth/signup"
  style="margin:0 16px 16px">${t('createAccount')}</button>
```

`.btn-block { width: 100% }` — و`100%` هنا هي **390** لأنّ الحاوية بلا حشوة
— **ثمّ يُضاف إليها هامشٌ 16 من كلّ جهة**، فيطلب الزرّ 422 في صندوق 390.
و`.app-main` فيها `overflow-x: hidden`، فالفائض لا يُمرَّر بل **يُقصّ**.

**مقيسٌ على 390×844، على النسختين وباللغتين** (صندوق الشاشة 0…390):

```
ar · index.html               الزرّ [-16 … 374]  عرضه 390   ← 16px مقصوصة يساراً
ar · index-single-file.html   الزرّ [-16 … 374]  عرضه 390
en · index.html               الزرّ [ 16 … 406]  عرضه 390   ← 16px مقصوصة يميناً
en · index-single-file.html   الزرّ [ 16 … 406]  عرضه 390
```

**وعلى المقاسات الأربعة** (إطار `.phone` يتوسّط فوق 460 بكسل):

```
390    [-16 … 374]        768    [138 … 598]
900    [205 … 663]       1280    [395 … 853]
```

في كلّ حالة: **16 بكسلاً خارج الإطار، والزرّ منزاحٌ 16 عن مركزه**.

**ولماذا يهمّ:** الزائر يصل إلى `#/settings` بقرار `195` (تفضيلات الجهاز
ليست ملك الحساب)، **وهذا الزرّ هو الباب الوحيد إلى التسجيل على تلك الشاشة**
— وهو الذي يقول له ما الذي يضيفه الحساب. وأثقل ما فيه أنّ القصّ ليس تمريراً:
**المقصوص لا يُوصَل إليه أصلاً**، وهي بعينها العبارة التي كُتبت في `V.06.4`
حين قُصَّت ثلاثة عناصر في اللوحة.

**وهو الموضع الوحيد من نوعه:** بحثٌ عن `btn-block` في `js/` يعطي **122
موضعاً**، وواحدٌ منها فقط يحمل هامشاً سطريّاً — هذا.

### ولماذا لم تمسكه الشبكة

`test_v41 · 2.5` مكتوبٌ لهذه الشاشة بعينها ولهذا الدور بعينه:

```js
overflow: document.documentElement.scrollWidth > 390,
…
ok('2.5 no sideways scroll at 390', !visitor.overflow);
```

⚠️ **و`.app-main { overflow-x: hidden }` تعني أنّ الصفحة لا تمرّر أفقيّاً
أبداً** — فهذا البند **لا يستطيع أن يحمرّ** على شيءٍ داخل منطقة المحتوى،
مهما قُصّ. مُثبَتٌ بالتجربة: وضعتُ عنصراً عرضه 1400 بكسل داخل شاشة
`#/magazine` وبقي البند **أخضر**. والقياس نفسه بالصيغة نفسها في
`v37 · 7` وفي غيرها.

**و`V.06.4` عرفت هذا وقاست الصناديق بيدها** («هذا قصٌّ لا تجاوز، وهو أثقل لا
أخفّ») — **لكن ذلك القياس لم يصر بنداً دائماً**، فعاد الباب مفتوحاً.

---

## 5) أسنان ما ادّعيته — مُثبَتةً لا مُدَّعاة

كلّ سطرٍ هنا: كُسر ما يحرسه فاحمرّ، وأُصلح فاخضرّ، **وأُرجع كلّ ما لُمس**.

| البند | ما كُسر | النتيجة |
|---|---|---|
| أ1 شاشة أقلّ من 40 حرفاً | `TermsScreen` تُرجع فارغاً | **FAIL** `#/terms(0)` |
| أ2 وسم HTML مطبوع كنصّ | اسم `b1` صار `<b>مطعم الشام</b>` (يمرّ بـ`esc`) | **FAIL** `#/home #/directory/b1` |
| أ3 مفتاح i18n ظاهر | `#/help` تطبع `'faqTitle'` بدل `t('faqTitle')` | **FAIL** `#/help[faqTitle]` |
| أ4 undefined على الشاشة | `AboutScreen` تُقحم `${S.state.__teeth}` | **FAIL** `#/about` |
| أ5 رابط بلا شاشة | `#/privacy` فيها `href="#/teeth-nowhere"` | **FAIL** `#/privacy→#/teeth-nowhere` |
| أ6 خطأ console | `CategoriesScreen` تقرأ خاصّية من `undefined` | **FAIL** `#/categories: PAGEERROR` |
| ب1 الرّد عن `#/boost/c1` | `ownsListing()` تُرجع `true` دائماً | **FAIL** بقي على `#/boost/c1` |
| ب2 الرّد عن `#/business/edit/b1` | `ownsBusiness()` تُرجع `true` دائماً | **FAIL** بقي على المسار |
| ج1 اللوحة تطلب كلمة | `unlocked = true` في `admin.js` | **FAIL** `tabs=8 user=false` |
| ج4 الكلمة غير مخزَّنة نصّاً | `adminAuth = { user, pass }` | **FAIL** |
| ج5 فيه salt وhash | نفس الكسر | **FAIL** `user,pass` |
| د لا تمرير أفقيّ | **مكسورٌ في المستودع كما هو** — والإصلاح المقترَح يخضّره | **FAIL 16 بنداً → أخضر بعد الإصلاح → أحمر بعد الإرجاع** |
| هـ صندوق خارج التغطية | `outsideBoxHtml()` تُرجع `''` | **FAIL** `#/home #/directory` |

**وبعد الإرجاع في كلّ مرّة:** `git status` فارغ، والقسم يعود أخضر
(أ 7/7 · ب 9/9 · ج 6/6 · هـ 2/2).

**وبند «د» أُثبت في الاتّجاهين على الشيفرة الحقيقيّة**: طُبِّق الإصلاح
المقترَح فصار الزرّ `[16 … 374]` عرضه **358** في اللغتين وصفر تجاوزٍ على
المقاسات الأربعة، **ثمّ أُرجعت الشيفرة فعاد الأحمر بالأرقام نفسها.** ومع
الإصلاح مطبَّقاً شُغِّلت السويتات التي تمسّه: `v41 26/0 · v37 16/0 ·
v46 31/0 · v48 28/0 · v4 103/0 · v5 134/0`.

---

## 6) ما قِيس اليوم وهو مؤجَّلٌ بقرار — لا يُبلَّغ عنه كأنّه جديد

| المؤجَّل | المقيس اليوم |
|---|---|
| **الإحداثيّات** — لا نشاط يحمل `lat`/`lng` | قِيس من `js/data.js`: **0 من 514**، ولا مفتاح `lat` على أيّ سجلّ. مطابقٌ لِما في `الحالة.md` |
| **الدرج يتجاوز حين تُفتح مجموعة** | مطويّاً **844/844 للدورين** (القاعدة قائمة). ومفتوحاً: زائر «الأقسام» **+181** و«المساعدة» **+131**؛ عضو **+106** و**+56**. مطابقٌ لِما في `الحالة.md` — لم يتحرّك |
| **`SUPPORT_PHONE` فارغ** | `js/store.js:4680` — `''` كما هو، بانتظار رقمٍ حقيقيّ من مالك البرنامج |
| **الصفحات القانونيّة مسوّدات · الدفع محاكى · الإشعارات بلا خدمة** | لم تتغيّر، ولا تُبلَّغ |

⚠️ **وملاحظةٌ على أداة الفحص الساكن نفسها، لا على البرنامج:** بند
`wiring.mjs · 7` يطبع `withCoords=34` و«الـ480 الباقية هي الصفوف
المستورَدة». **والرقم 34 ليس أنشطة**: الأداة تعدّ `lat:` بتعبيرٍ نمطيّ عبر
`data.js` كلّه، فتلتقط مراكز الأكواد البريديّة ونقاط المدن. `الحالة.md`
صحّحت هذا عندها بالفعل (**514 من 514 بلا إحداثيّات**)، **ونصّ الأداة ما زال
يقول القديم** — وهو سطرٌ يُقرأ في أوّل كلّ فحص. تصحيحه سطرٌ في ملفٍّ مرقَّم،
ولا يُلمَس من هنا.

---

## 7) الإنذارات الكاذبة التي وقعتُ فيها أنا — لجلسة الغد، لا لمالك البرنامج

هذا القسم هو الذي يمنع تكرار نصف ساعة.

1. ⚠️ **أوّل قياس عرضٍ كتبته كان بلا أسنان، وبالطريقة نفسها التي أخفت
   العطل.** كتبت `documentElement.scrollWidth > innerWidth`، ثمّ حقنت
   عنصراً عرضه 1400 بكسل لأثبت أسنانه — **فبقي أخضر.** السبب أنّ
   `.app-main` فيها `overflow-x: hidden`. **فلا تقس التجاوز الأفقيّ بمقياس
   الصفحة في هذا البرنامج أبداً؛ يُقاس صندوق العنصر مقابل إطار الشاشة.**
   وهذه بعينها هي الثغرة التي أبقت `v41 · 2.5` أخضر فوق زرٍّ مقصوص.
2. ⚠️ **أوّل محاولةٍ لإثبات أسنان «خارج التغطية» كانت مرآةً لا فحصاً.**
   غيّرت `outsideTitle` في `i18n.js` لأكسرها — **وفحصي يقرأ توقّعه من
   `i18n.js` نفسه**، فتغيّر التوقّع مع النصّ وبقي أخضر. الكسر الصحيح كان في
   **شرط الرسم** (`outsideBoxHtml`)، لا في النصّ. **وما يحرسه هذا البند هو
   حضور الصندوق، لا حرفيّة جملته** — ومن أراد حراسة الجملة فمكانها
   `chk_i18n`.
3. ⚠️ **حدود بند «مفتاح i18n ظاهر»، مكتوبةً صراحةً:** يمسك مفتاحاً
   **موجوداً في الحزمتين** ويُطبَع اسمه بدل قيمته. أمّا مفتاحٌ غير معرَّف
   أصلاً — `t('لا-يوجد')` — فلا يستطيع فحصي أن يعرف اسمه، **ويمسكه الفحص
   الساكن** (`wiring · 1` «كلّ مفتاح `t()` معرَّف»)، وهو أخضر اليوم.
   البندان معاً يغطّيان الحالتين، وأيٌّ منهما وحده لا يغطّيهما.
4. ⚠️ **`s ج2/ج3/ج5` احمرّت مرّةً واحدة في تشغيلةٍ كاملة** (اللوحة لم تفتح
   بعد ضبط الكلمة على النسخة الملفّ-الواحد) **وخضرّت في تشغيلتين بعدها
   منفردةً ومجتمعة.** لم أطارده: هي بصمة ضغطٍ على آلةٍ بنواتين، وهو
   السابقة نفسها المسجَّلة لـ`v28`/`v31`/`v37`. **لكنّي لم أتركه للتخمين**:
   أضفتُ إلى فحصي قراءة سبب الرفض من `#e_aNew` عند الفشل، فإن تكرّر غداً
   يُقرأ السبب ولا يُخمَّن.
5. **`#/my-requests` مسارٌ ساكنٌ في `app.js` وليس في قائمة الشاشات الـ41**
   التي أُعطيت لي. ليس عطلاً — `v37` يمشي عليه — لكنّه فرقٌ بين قائمةٍ
   وقائمة، وأثبتُّه هنا لئلّا تحسبه جلسةٌ بعدي شاشةً بلا تغطية.

---

## 8) دليل الواصل الجديد

**لا شيء اليوم يمسّ نصّه.** العطل الوحيد الخارج هو في `js/screens/profile.js`
على شاشة الإعدادات، ولا علاقة له بـ`js/newcomer-content.js` ولا بمصدره في
`tools/nc/`. والملفّ `030` ما زال في «ما ينتظر الإرسال» كما هو.

---

## 9) ما لم يُلمَس

- **لم تُعدَّل شيفرة، ولم يُدفَع commit، ولم يُفتَح فرع.**
- كلّ ما لُمس أثناء إثبات الأسنان أُرجع بـ`git checkout`، و
  `index-single-file.html` أُعيد إلى ما كان — و`git status --porcelain`
  فارغٌ في آخر الجلسة.
- `docs/الحالة.md` و`docs/سجل-القرارات.md` قُرِئا ولم يُكتب فيهما حرف.
- لا تقرير تحت `docs/تقارير/` أوّل سطره `الحالة: جديد` — لا شيء كان بانتظار
  القراءة.
- أداةُ فحصي تعمل **خارج المستودع** ولم تُكتَب فيه.

---

## 10) ما يُرسَل مع هذا التقرير

**ملفّ إصلاح واحد** — «زرّ إنشاء الحساب على `#/settings` مقصوص». **رقمه
يضعه مالك البرنامج**، ولا رقم نسخةٍ فيه، ولا رقعة جاهزة: مواصفةٌ ينفّذها كودٌ على
نسخته.
